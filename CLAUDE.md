# MDF-Viewer – Project Context for Claude Code

## Project Overview

MDF-Viewer is a desktop application for visualizing ASAM MDF measurement data files (MDF3 and MDF4). It is a greenfield rewrite based on the author's prior experience with a working prototype. The goal is a clean, maintainable architecture from the start – the prototype suffered from tight coupling between data, UI, and plotting components.

The application is developed as a free, open-source project, targeting individual engineers and automotive measurement professionals.

**GitHub:** https://github.com/andalf-74/MDF-Viewer (public)

**Target platforms:** Windows, Linux  
**Language:** Python  
**Key libraries:** PyQt (UI), PyQtGraph (plotting), asammdf (MDF file I/O)

---

## Architecture Philosophy

Strict MVC separation is mandatory:

- **Model** – Pure data, no UI knowledge. Signal samples, timestamps, metadata.
- **View** – Pure UI, no business logic. Plots, widgets, dialogs.
- **Controller** – Coordinates between Model and View. Manages active signals, cursor state, selection state.

The prototype's core problem was that data classes, viewer, and plotter were too tightly coupled. This rewrite must not repeat that mistake. Every architectural decision should reinforce this separation.

### Signal Data Model

Three distinct signal classes were identified from the prototype (`SignalData`, `SignalMetadata`, `ActiveSignal`) — see `docs/architecture.md`'s "Signal classes" section for the definitions (module paths, field ownership), which is the source of truth.

---

## Project Structure

```
src/mdf_viewer/
    model/          # Pure data — no Qt/PyQtGraph imports
    view/           # Pure UI — no business logic
    view_model/     # ActiveSignal: bridges model data with plot objects
    controller/     # Coordinates model ↔ view
    plugin_api/     # PluginContext facade — the only thing a plugin may import (#71)
tests/
    model/
    view/
docs/
pyproject.toml      # src-layout, entry point mdf-viewer
requirements.txt / requirements-dev.txt
```

`.gitignore` covers Windows and macOS development environments.

See [`docs/ui.md`](docs/ui.md) for full UI layout (menus, toolbar, panels, info boxes).  
See [`docs/api.md`](docs/api.md) for per-module API reference and implemented module table.  
See [`docs/architecture.md`](docs/architecture.md) for architectural decision log.  
See [`docs/release.md`](docs/release.md) for release build instructions.

---

## File Handling

- **Single file only (MVP)** – loading a new file replaces the current one
- **Recently opened files** – up to 4 entries persisted in `settings.json`; shown in File menu; stale paths pruned silently on menu open; failed loads are not recorded
- **Session persistence is manual, not automatic** – the app always starts fresh (no auto-restore of the last session), but a full workspace session — every tab (name, plot|AST divider width, Active Signals Table column widths), every tab's plot-stripe layout, every active signal's placement/colors/axis grouping/zoom/cursor/selection, and the full loaded-measurement pool (paths, short names, offsets, Primary, Sync state) — can be saved to and restored from a `.mvc` file via File → Save Workspace / Save Workspace As… (#37, #77, extended to the full workspace by #106). Auto-restore-on-startup is not implemented.
- **Robust error handling is mandatory** – the application must never crash on malformed, incomplete, or unexpected MDF content; errors must be caught and communicated to the user gracefully

---

## MDF Support

- MDF3 and MDF4 via the `asammdf` library
- All available channel groups and signals must be represented in the Signal Browser (a flat, cross-measurement list as of #103 — see `docs/ui.md`/`docs/api.md`; every channel is still shown, channel-group membership is now a hover tooltip rather than list structure)

### Known asammdf bug — `__del__` on corrupt files

When `asammdf.MDF(path)` fails to open a corrupt file, it raises an exception midway through `MDF4.__init__` before assigning `self._file`. Python's GC later calls `MDF4.__del__`, which calls `close()`, which unconditionally accesses `self._file` — triggering:

```
Exception ignored while calling deallocator <function MDF4.__del__ ...>:
AttributeError: 'MDF4' object has no attribute '_file'
```

This is printed to stderr but does **not** affect app behaviour — `MdfLoader.open()` correctly raises `MdfLoadError` and the error dialog is shown. **Do not chase this as our bug.** The fix belongs in asammdf's `close()` method (guard with `getattr(self, '_file', None)`). We have no clean way to suppress it from our side without redirecting stderr.

---

## Todo / Future Features (not MVP)

- Multi-file support with multiple X-axes and synchronization (by timestamp overlap, manual time offset, or signal-based alignment)
- Auto-restore last session on startup (manual save/load via `.mvc` files already implemented, see File Handling above)
- Additional toolbar and menu items (TBD)

---

## Development Workflow

### Reviewing Issues
When asked to look at / check / review the GitHub issues, always fetch and display them grouped by milestone so the current development priority is immediately visible.

### Grill-Me Skill
"Grill me" about a feature or topic uses the `grilling` skill for the interview mechanics. In this repo, the findings must be written into the relevant `docs/requirements/*.md` file(s), not just left in the conversation — for a Feature issue, this *is* the "write requirements before implementation" step from Issue Triage below, not a separate later task.

### Requirements Workflow

`docs/requirements/` is the single source of truth for **what the app does** — distinct from GitHub issues (a bug/idea reservoir where anything gets jotted down) and from `docs/architecture.md` / `docs/ui.md` (the **how**, i.e. MVC/View implementation detail). Put another way: **the code is the source of truth for what the app currently does; the requirements docs are the source of truth for what it should do.** When the two disagree, that disagreement is a Bug (see Issue Triage below) — either the requirements doc was silent, or the code drifted from a requirement that was correct.

- **Structure** – one file per capability/domain (e.g. `file-handling.md`, `mdf-support.md`), not one monolithic file, and not split by UI-vs-non-UI — that split is an architecture-stage decision, not a requirements-stage one.
- **Style** – prose per section; each individually-testable sentence is tagged inline with a stable ID, `REQ-<DOMAIN>-NNN` (e.g. `REQ-FILE-010`), numbered in steps of 10 per sub-topic so new requirements can be inserted later without renumbering. Every tagged sentence must be: **positive** (state what the system does, not what it doesn't — a negative phrasing usually means it hasn't been pinned to an actual observable behavior yet), **clear** (stands on its own without the reader having to untangle it), and **testable** (corresponds to something a test can assert). A sentence that's purely explanatory or cross-references other REQ-IDs without making its own independent claim is not a requirement — leave the prose in place but don't give it a REQ-ID.
- **Traceability** – tests cite the requirement they verify via `@pytest.mark.requirement("REQ-FILE-010")`. Not every requirement needs one, though: a `REQ-ID` with no citing test isn't automatically a coverage gap — some requirement classes are legitimately verified by a repeatable *process* instead of an automated test (e.g. `REQ-NFR-040`, "runs on Windows," is verified by the fact that every release goes through a real build, a real installer, and the user running the resulting binary — the same "only a human running the real thing proves it" pattern as the mouse-interaction live-testing rule above). Before treating an untested `REQ-ID` as a gap, check whether it's actually one of these, a wording problem (see Style above), or a genuine missing test.

First file drafted: `docs/requirements/file-handling.md`.

### Issue Triage

GitHub issues are the only backlog and arrive in different flavors. **Before starting work on an issue, state which flavor it is and get the user's agreement** — don't silently assume it, since the flavor decides which docs must stay in sync. The full flavor taxonomy and which docs each flavor must update (`docs/requirements/`, `docs/architecture.md`, `docs/ui.md`, `docs/api.md`, `CHANGELOG.md`) is the source of truth in `docs/agents/issue-tracker.md`'s "Issue Triage flavors" section — follow it before closing any issue.

### Plugin vs. Built-in Decision Rule
Before implementing any new feature, ask: **does this belong in the base app, or should it be a plugin?**

A feature belongs in the base app if it is core to the viewing experience — without it, the app cannot fulfil its primary purpose (open a file, browse signals, plot them).

A feature should be a plugin if it is optional, self-contained, and adds capability on top of the core without the core needing to know about it. Ask:
- Can the base app still function usefully without this feature? → plugin
- Does implementing it require the `PluginContext` API to grow substantially to accommodate it? → probably built-in (the API complexity cost is too high)
- Is it something a user might reasonably want to disable or replace? → plugin

### General Rules
- **Always check the codebase first** – before making assumptions or proposing solutions, check whether the answer already exists in the codebase
- Always propose architecture and structure before writing code
- Ask clarifying questions when requirements are ambiguous
- Write tests alongside implementation, not after
- Prefer explicit, readable code over clever one-liners
- All user-facing strings should be in English (internationalization not in scope for MVP)
- Commit messages should be clear and descriptive

### Verifying Mouse/Interaction Changes (#78 postmortem)

Synthetic Qt mouse-event tests (`QTest.mousePress/mouseMove/mouseRelease`, or directly calling `eventFilter(...)` with a hand-built event) are **not sufficient evidence** that a fix touching mouse routing, event filters, or Z-order/stacking actually works. They inject events below the real OS input pipeline and can pass while the real interactive app does not — confirmed during the #78 (delta-time line undraggable) investigation, where two separate fixes each passed dedicated regression tests and a full-app `QTest`-based repro, yet failed in a live debug session. Swapping `QT_QPA_PLATFORM=offscreen` for the real platform plugin did **not** close that gap either — `QTest` bypasses real OS-level input regardless of platform.

The project does not have OS-level GUI automation (e.g. `pywinauto`) and isn't investing in it for now — that decision can be revisited if this class of bug keeps recurring. Until then: **for any change touching mouse interaction, drag handling, event filters, or Z-order/stacking in `view/plot_area.py` or `view/cursors.py`, remind the user to manually test the live app before considering the change verified** — do not rely on automated tests alone for these areas, even when they pass. Logic/data tests remain valuable and should still be written as usual; this only applies to the "does the interaction actually reach the right widget" layer.

**Resolution (2026-07-01):** #78 is fixed — along with #80 (Z-order broken on shared Y-axis) and #81 (some signals not selectable), which turned out to share related causes. What finally worked, after two more theoretical fixes still failed live: temporary `print()` diagnostics added directly to `PlotArea.eventFilter`/`CursorView`, with the user reproducing in their real running app and pasting console output. That ruled out the event-routing plumbing as broken and pointed to the actual (unrelated) cause — `pi.vb`'s autoRange tracking the cursor/delta lines' own bounding boxes because they were added without `ignoreBounds=True`. Lesson reinforced, not just theorized: when a live-only mouse bug resists two rounds of "looks right in tests," add print statements to the real code path and get real console output before trying a third theoretical fix — don't remove that logging until the user has personally confirmed the fix live, even after you're confident you've root-caused it.

### PyQtGraph/Qt Object Teardown Pitfall (#120, 2026-07-10 postmortem)

A native segfault (`EXCEPTION_ACCESS_VIOLATION`, zero Python traceback) was chased down to a recurring pattern: code that tears down a `ViewBox`/`AxisItem`/curve called `layout.removeItem()` / `scene.removeItem()` / `.hide()` and stopped there — none of those destroy the underlying Qt object. The item stays alive, orphaned, still wired into whatever signal/slot connections it had (e.g. cross-stripe X-range sync), and can crash much later when something unrelated touches it. Found three separate instances of this in `plot_stripe.py` (`_destroy_vb_and_axis`, `set_measurement_axes`, `set_axis_padding`'s spacer) plus a fourth, different-shaped leak in tab-close (`AppController.remove_tab` never ran a closed tab's still-active signals through `remove_signal`, and `MainWindow._on_tab_close_requested` never `deleteLater()`'d the closed tab's page widget — `QTabWidget.removeTab()` does not delete the widget). See `docs/architecture.md`'s "PyQtGraph/Qt Teardown Pitfall" decision-log entry for the full technical detail (including the `getViewBox()`/`viewRangeChanged()` crash mechanism) and what to check for in new code.

**How it was actually found — a different technique than the #78 postmortem above:** synthetic reproduction wasn't possible (this needed real signal data, real multi-stripe layout, and a specific sequence of user actions to trigger), and the crash left no Python traceback for print-instrumentation to directly bracket at first. What worked: (1) launching the real app repeatedly and matching Windows' own crash dumps (`%LOCALAPPDATA%\CrashDumps\python.exe.<pid>.dmp` — no WinDbg/cdb installed, so parsed with the pure-Python `minidump` pip package) against the *timing* of the user's live reproduction, using `ProcessCreateTime` from the dump to confirm which dump actually corresponded to which repro attempt (an early assumption that a dump was from an unrelated crash turned out to be wrong once the user gave a precise timestamp to check against); (2) once the exact exception (null-pointer read, same code offset every time) was confirmed as the real target, adding synchronous `print(..., flush=True)` statements immediately before *and* after each suspect call, so the crash's exact location could be narrowed to "ran the before-print but never the after-print for call N" across several rounds of live reproduction; (3) getting the user to vary the *exact sequence* of UI actions (not just "click the same button again") surfaced the real trigger condition, which turned out to be a specific state transition (a stripe's computed axis padding dropping to exactly 0 for the first time in the session) that three prior code-reading passes had missed.

### Branching & Release Policy (#20)

**Lazy release-branch model** — chosen for low overhead given infrequent releases and a small team:

- **`main`** is the trunk. All bugfixes and features land here (directly or via PR), and each release is tagged `vX.Y` on `main`.
- A **`release/X.Y`** branch is created only when it's actually needed: you've started work on a feature for the *next* release on `main`, but still need to ship a bugfix to the *currently released* version (which must not receive that in-progress feature). Cut `release/X.Y` from the `vX.Y` tag at that point.
- Bugfixes to a released version go to its `release/X.Y` branch, get tagged `vX.Y.Z`, and must be cherry-picked forward to `main` so the fix isn't lost in the next release.
- If no feature work is in flight on `main` when a bugfix is needed, just commit the fix directly to `main` and re-tag/patch-release from there — no branch needed.

`master` was renamed to `main` as part of adopting this policy.

### Version Bump Checklist

`pyproject.toml` reads its version dynamically from `__init__.py`, so only **two** files need updating at release time:

1. `src/mdf_viewer/__init__.py` — `__version__ = "X.Y"`
2. `installer/mdf_viewer.iss` — `#define AppVersion "X.Y"` (line 8)

See `docs/release.md` for the full build and publish steps.

---

## Current Status

**As of 2026-07-29: v2.3.1 is the latest released version** (https://github.com/andalf-74/MDF-Viewer/releases/tag/v2.3.1, tag `v2.3.1` on `main`). 2206 tests passing. Ships `MDF-Viewer-2.3.1-Setup.exe` (installer) and `MDF-Viewer-2.3.1-Windows.zip` (portable) — see `docs/release.md`. Fix/feature-by-feature detail lives in `CHANGELOG.md`'s `[2.3.1]` entry and this file's per-issue status entries below, not repeated here. (Prior release: v2.3, 2026-07-26, 2125 tests.)

2.2 combines two closed GitHub milestones, both reshuffled from earlier planning (see git history for the full renumbering story if needed): **"2.2 Multiview/Multimeasurement"** (#17 umbrella + #97–#106, #119, #122, #124, #130, #131, #133 — multi-file support, Plot Stripes, tabs, per-stripe AST, workspace/config extension, etc.; #104 rejected by user directly) and **"2.2 Additional Bugfixing"** (#134–#140 — a later architecture review's findings plus two more issues added afterward; #139 closed as duplicate/already-implemented, #140 fixed). Both milestones and their umbrella issues are closed on GitHub.

**2.3 Plugins is now fully closed on GitHub (27/27 issues)**, confirmed 2026-07-27: #43 (umbrella), #70 (event bus on AppController), #71 (PluginContext API facade), #72 (Plugin base class/lifecycle), #73 (UI extension points in MainWindow), #74 (plugin loader/discovery), #75 (proof-of-concept built-in plugin), #76 (convert update checker into a first-party plugin), #147 (virtual measurements/signals — plugin groundwork for artificial-signal and custom-file-format plugins), #148 (register_tab_type — pluggable tab types), #150 (rescan/reload plugins without restarting), #151 (plugin-author SDK package, a separate repo), #152 (PoC plugin exercising virtual measurements, follow-up to #147), #159 (Plugin Preferences API — per-plugin settings + preferences UI), #160 (Plugin Overview — per-plugin enable/disable) all done — see below for detail. #118 (OpenStreetMap View) is not yet started; retitled "as a first-party plugin" and moved to the Backlog milestone as of 2026-08-01 (user's call — it's better framed as a plugin than core-app work), not 2.3 Plugins. #141 (Tree View selectable by Preferences), in 2.3.5 Further Polishing, is done — see below.

One active milestone remains directly downstream of the 2.3 Plugins effort:
- **2.3.1 Misc Features/Fixes** — smaller user-facing asks collected separately from the 2.3 Plugins effort: #117 (configurable background color, done), #142 (Y autozoom of selected signal, done), #143 (import/export a list of labels, done, see below), #146 (improve cursor movement by arrow keys, done), #153–#158 (UI-polish cluster: accent color, panel section typography, Info panel field grouping, monospace numerals, a UI-restraint guideline doc — all done, see below), #156 (CN comment display bug, done), #160 (done, see below), #161 (installer default-option settings, done, see below), #162 (Plugin API: attach/detach on an already-registered virtual measurement, extending #147, implemented and live-tested — see below), #164 (AST name-column elision bug, done, see below), #165 (menu bar icon redesign, done, see below). #141 (Tree View selectable via Preferences) has since moved to the 2.3.5 Further Polishing milestone. 2.3.1 Misc Features/Fixes is now fully closed as of 2026-07-29.

**#71 (PluginContext API facade) implemented, committed (`eb63eee`), pushed, and closed 2026-07-13** via a 6-milestone plan (types → registry → AppController additions → read surface → registration/event surface → e2e harness + docs), full grill-me → requirements (`docs/requirements/plugin-api.md` REQ-PLUGIN-060–150) → architecture (Plan-agent-reviewed, `docs/architecture.md`) workflow. New top-level `src/mdf_viewer/plugin_api/` package (`types.py`/`registry.py`/`context.py`) — `PluginContext` is the only object a plugin will ever import; read-only signal/measurement/cursor projections, `register_menu_action`/`register_dock_widget` (stubs, rendered by #73), event subscription (`subscribe`/`unsubscribe_all`). `AppController` gained `all_workspaces()`, `active_tab_index`, and an opaque per-signal token (`token_for_signal`/`find_active_signal_by_id`) — not `id(active_signal)`, which would be unsafe for a handle a plugin can hold indefinitely. Filed follow-up [#147](https://github.com/andalf-74/MDF-Viewer/issues/147) for a future "virtual measurement" capability (artificial-signal and custom-file-format plugins), out of #71's scope. No CHANGELOG entry (no user-visible surface yet, matching the #70 precedent).

**#72 (Plugin base class and lifecycle) implemented 2026-07-13** via a 4-milestone plan (base class → PluginContext._teardown() → start()/stop() lifecycle+auto-wiring → docs), same full grill-me → requirements (REQ-PLUGIN-160–190) → architecture (Plan-agent-reviewed) workflow, extending #71's package with `plugin_api/plugin.py`. `Plugin` is what a plugin author implements against (`PluginContext` is what it *receives*): class-attribute metadata (`name` enforced non-empty via `__init_subclass__`, not `__init__`, so a subclass that forgets `super().__init__()` can't skip it), mandatory `activate(context)`/optional `deactivate()`, five no-op event handler methods auto-wired to `PluginContext.subscribe()` only when overridden (no manual subscribe call needed), and framework-facing `start()`/`stop()` lifecycle methods (idempotent, called by the future loader #74) — `start()`'s failure path tears down via a new `PluginContext._teardown()` before returning `False`, so a plugin whose `activate()` registers something then raises never leaks that registration forever. 1781 tests passing (up from 1762 at #71). No CHANGELOG entry (same reasoning as #71). Committed (`91372a3`), pushed, and closed.

**#73 (UI extension points in MainWindow) implemented 2026-07-13** via a 4-milestone plan (AppController.plugin_registry → Plugins menu → docked-widget sections → docs), same grill-me → requirements (REQ-PLUGIN-200–231) → architecture (Plan-agent-reviewed) → plan-mode milestones workflow. Makes #71's registration stubs visible: a "Plugins" menu (between Edit and Help, hidden entirely when the registry is empty — today's actual state) built once in `MainWindow.set_controller()`; docked-mode dock widgets stack into #98's existing Info/Properties splitter as another titled pane; dialog-mode widgets get an auto-added "*Title*…" Plugins-menu entry, built once and cached. Plan review caught a fatal wiring bug before implementation (`MainWindow` is constructed before `AppController` in `app.py`, so the registry can't be read from `__init__` — moved to `set_controller()`), and that very fix then silently invalidated an unrelated placement assumption from the same review (plain `addMenu()` no longer lands between Edit/Help once built later — fixed with `QMenuBar.insertMenu()`), caught during implementation itself. `MenuActionRegistration.invoke()`'s return type widened from `None` to `bool` (#71's shipped code) so a failed menu action can show a status-bar message. 1794 tests passing (up from 1781 at #72). `docs/ui.md`/`docs/api.md` updated. No CHANGELOG entry (registry stays empty until #74/#75 exist).

**#74 (plugin loader and discovery) implemented 2026-07-13** via a 4-milestone plan (Settings.plugins_dir → loader import machinery → load_all/deactivate_all → app.py wiring + docs), same grill-me → requirements (REQ-PLUGIN-240–280) → architecture (Plan-agent-reviewed) → plan-mode milestones workflow. New `plugin_api/loader.py`: `PluginLoader` scans a plugins directory (one subfolder per plugin, each declaring an explicit `PLUGINS: list[type[Plugin]]` — no introspection, cleanly supports single-plugin and "toolsuite" packages alike), imports each via `importlib.util.spec_from_file_location`, activates every declared class, and deactivates them all on shutdown. Plugins directory defaults to next to the running app (installer/portable — travels with a portable copy) or relative to the source checkout in dev mode, overridable via new `Settings.plugins_dir`. Wired into `app.py` between `AppController` construction and `window.set_controller()` (the hard ordering constraint #73 imposed), with `deactivate_all()` after `app.exec()` returns. Plan review caught a correctness bug that would have broken every multi-file plugin package: `importlib`'s module must be registered in `sys.modules` *before* `exec_module()` runs, or a package's own `from . import sibling` fails — confirmed fixed via a dedicated regression test. 1818 tests passing (up from 1794 at #73). Also fixed, mid-implementation, a second recurrence of the "truncated Read splices into existing code" mistake (this time silently removing `Settings.check_for_updates`'s `_save()` call) — caught by the full test suite, not by inspection; see `docs/architecture.md`/memory for detail. No CHANGELOG entry (no real plugin ships until #75).

**Bug #149 found and fixed 2026-07-13, while scoping #75** (before writing any plugin code — checking whether the Signal Statistics PoC example would receive what #71's own requirements already promised). `PluginContext.subscribe()` forwarded the raw `EventBus` payload to plugin handlers completely untransformed: every event's `tab` field is the raw `TabWorkspace` (exposing that tab's actual `plot`/`cursor_ctrl`/`zoom_ctrl`), and `signal_added`/`signal_removed`/`selection_changed` carry the live, mutable `ActiveSignal`(s) — a plugin could corrupt real in-memory sample data in place, desync the app's rendered state, or manipulate live PyQtGraph objects directly (risking the exact native-crash class #120 already fixed once). Fixed by adding 5 read-only `Plugin*Event` dataclasses (`tab_index: int | None` instead of the raw tab; `PluginSignalView` instead of the raw `ActiveSignal`), an explicit `_translate_event()` with a loud `AssertionError` fallback (never a silent passthrough), and a structural guard test cross-checking `_KNOWN_EVENTS` against `EventBus`'s real `pyqtSignal` attributes — so a future new event without a matching translator fails a test immediately. Also reordered `AppController`'s 4 signal-removal sites so `signal_removed` fires *before* the plugin-facing token is dropped, closing a related leak. 1821 tests passing (up from 1818). Full detail in `docs/architecture.md`'s "Event Payload Leak (#149)" entry.

**#75 (proof-of-concept built-in plugin, Signal Statistics) implemented 2026-07-13.** New `plugins/signal_statistics/__init__.py`: subscribes to `selection_changed`, shows Min/Max/Mean of the selected signal in a docked widget via `context.get_samples()`. Deliberately dev-mode-only, not packaged into the release build — lives directly at `<repo root>/plugins/`, which is already #74's own default dev-mode discovery directory, so it's auto-discovered with zero extra wiring; shipping it in the installer/portable build was considered and dropped since none of the PoC's actual goals (validate the lifecycle, surface API gaps, serve as a reference example) need it to reach an end user's installed copy. Tests load it through the real `PluginLoader` pointed at the real repo `plugins/` directory, not a direct import — proving the actual committed file works via the actual discovery mechanism. 1824 tests passing (up from 1821 at #149). **Live-tested in the real running app — user confirmed it opens normally and the panel shows and updates correctly** — the first time the full pipeline (#71→#74, #149) has been exercised end-to-end with a real plugin, not just mocked-piece unit tests.

**#147 (virtual measurements/signals, plugin groundwork) implemented 2026-07-14** via a 5-milestone plan (core model-layer types → AppController/EventBus wiring → `.mvc` capture_config() rework → PluginContext/Plugin surface → UI badges/guards), full grill-me → requirements (`docs/requirements/virtual-measurements.md` REQ-VMEAS-010–440, `docs/requirements/plugin-api.md` REQ-PLUGIN-290–310) → architecture (two Plan-agent review passes — one on the technical design, 7 real gaps found and folded in; one on the milestone breakdown itself) → plan-mode milestones workflow. The grill-me reframed the issue from "a plugin registers a measurement" into a general `VirtualSignal`/`VirtualMeasurement` abstraction: new `model/measurement_loader.py` (`MeasurementLoader` `Protocol`, satisfied by both `MdfLoader` and the new `VirtualMeasurementLoader`), `model/virtual_signal.py`/`model/virtual_measurement_loader.py`, and `LoadedMeasurement.owner_plugin: str | None` (sole source of truth for "is this measurement virtual"). New `PluginContext` methods `create_virtual_signal`/`create_virtual_measurement`/`attach_virtual_signal`/`register_virtual_measurement`, a new `measurement_closed` EventBus event/`Plugin.on_measurement_closed` handler, and a `(virtual)` badge in the Signal Browser and Measurement Info Box (Replace… disabled for a virtual measurement, REQ-VMEAS-440). The most serious gap the first Plan review caught: `.mvc` save computed `primary_index`/signal indices against the *unfiltered* pool while filtering only the saved measurement list — would have silently corrupted any save containing a virtual measurement; fixed by consistently filtering to `real_measurements` throughout `capture_config()`/`_capture_tab()`, widening `primary_measurement_index` to `int | None`. Also fixed, discovered during implementation: `ConfigManager.load()`'s `int(data.get("primary_measurement_index", 0))` would have raised on a saved `null` rather than restoring "no real Primary" — caught before it could ever ship. Downsampling/lazy-loading of real signals and full `.mvc` serialization of virtual measurements are explicitly deferred to future issues; no PoC/reference plugin ships in #147 itself (matches the #71–#74 precedent). Committed (`d2211fe`), pushed, and closed on GitHub; follow-up PoC issue filed as [#152](https://github.com/andalf-74/MDF-Viewer/issues/152), the same way #75 followed #71–#74. 1887 tests passing (up from 1824 at #75). `docs/api.md`/`docs/ui.md` updated. No CHANGELOG entry (no end-user-visible surface until a real plugin uses this, same reasoning as #71–#74).

**#148 (pluggable tab types, `register_tab_type`) implemented 2026-07-15** via a 5-milestone plan (foundations → minimal test-fixture plugin → tab creation UI → existing tab-lifecycle fixes → `.mvc` persistence), full grill-me (lighter than #147's — the issue body already recorded real design decisions from prior discussion) → requirements (`docs/requirements/plugin-api.md` REQ-PLUGIN-320–352) → architecture (**three** Plan-agent review passes — two on the technical design, one on the milestone breakdown; 17 real gaps found total and folded in) → plan-mode milestones workflow. Extends #71–#73's UI extension points: `PluginContext.register_tab_type(type_id, display_name, view_factory)` lets a plugin register a whole tab *template*, not just a menu action or dock widget — `AppController`'s `TabWorkspace`/`_workspaces` stays completely plot-tabs-only and unaware non-plot tabs exist, matching the boundary #73 already established for dialog-mode dock widgets. New dev-mode-only `plugins/tab_type_fixture/` (mirrors #75's precedent) exists purely to make the tab-lifecycle bug surface live-testable, since unit tests alone weren't sufficient here (unlike #147). The first design draft's approach — computing a workspace index by *counting* plot pages up to a `QTabWidget` position, redone ad hoc per call site — turned out to have 12 gaps across the first review, several serious (a `restore_config()` bug that would have silently misapplied one tab's saved axis-grouping/zoom/cursor state onto an unrelated tab; a reintroduction of the #130 native-crash class in the tab-close parking decision; `AttributeError` crashes in Preferences with any non-plot tab open). The redesign replaced all counting with **identity-based lookup** (new `AppController.tab_index_for_plot(plot_area)`, mirroring the existing `reorder_tabs()`/`_measurement_index()` identity-search idioms already in the codebase) plus an explicit `resolved_workspaces` correspondence built once during `.mvc` restore's Phase 2 and threaded through to Phase 4 — a second review found 5 more loose ends in that redesign (all folded in), and the milestone-breakdown review found 2 more call sites needing the same treatment (tab-bar drag-reordering; confirmed Ctrl+Tab needed no change). 1937 tests passing (up from 1887 at #147). Live-tested in the real running app at two checkpoints — tab creation/switch/close/duplicate/copy/reorder/Preferences with mixed plot and non-plot tabs (M4), and a full `.mvc` save→reload round-trip with a non-plot tab positioned between two plot tabs, confirming each plot tab's signals/axis-grouping/zoom/cursor state landed on the correct tab and the focused non-plot tab was restored correctly (M5) — both user-confirmed. Serialization of a non-plot tab's own internal content is explicitly deferred to a future issue (v1 persists only existence/name/view_type, recreating an empty instance on restore); #118 (OpenStreetMap View) remains the first real consumer, not yet started. `docs/api.md`/`docs/ui.md` updated. No CHANGELOG entry (no end-user-visible surface until #118 or another real plugin ships a tab type, same reasoning as #71–#75/#147).

**Bug #145 (raster mislabeled "variable" on signals with dropped frames) fixed 2026-07-15** (`fe24956`, latest commit on `main`). `_compute_raster()` used the mean sample interval, which occasional dropped frames (common on real bus signals) could drag off the true raster; switched to the median interval as the raster candidate, tolerating gaps that are clean integer multiples of it. `CHANGELOG.md`'s `[Unreleased] → Fixed` section has the user-facing entry.

**#150 (rescan/reload plugins without restarting) implemented 2026-07-23** via a 5-milestone plan (`PluginLoader` core mechanics → view-layer plumbing as a behavior-preserving refactor → Rescan trigger → Reload trigger, split apart by risk → docs/CHANGELOG), full grill-me (ten product decisions) → requirements (`docs/requirements/plugin-api.md` REQ-PLUGIN-360–401, also revising REQ-PLUGIN-200/211/321) → architecture (one Plan-agent review on the technical design finding 9 gaps, a second on the milestone breakdown finding 6 more) → plan-mode milestones workflow. Adds `PluginLoader.rescan()` (shares `load_all()`'s scan method; skips an already-active plugin's folder *before* import is even attempted, fixing a latent bug where a second scan would have double-activated it) and `reload_one(name)` (stops a plugin, purges its `sys.modules` entries by prefix and its on-disk `__pycache__` — the latter a real bug the M1 test suite caught live: Python's timestamp-based bytecode-cache invalidation truncates to whole seconds, so two rapid rewrites of the same plugin file within one second silently re-served stale bytecode — then re-imports and reactivates from disk; no rollback if the fresh copy fails to activate). The Plugins menu is now always visible (reversing #73's original "hidden when empty" rule) with "Rescan Plugins" and a "Reload Plugin" submenu at the top. `MainWindow` gained scoped, add-only dock-section tracking (`_sync_plugin_ui`/`_teardown_plugin_ui`) instead of the first design draft's "tear down and rebuild every plugin's UI on every click," which would have silently discarded live widget state for every untouched plugin — caught by the architecture review, along with a tab-closing loop that would have corrupted indices past the first close (fixed by closing top-down). Live-tested in the real running app at two checkpoints, split by risk — Rescan-only (new-folder discovery via the existing `plugins/tab_type_fixture/`/`plugins/signal_statistics/` dev fixtures, and retry of a deliberately-broken-then-fixed folder), then Reload-only (docked-panel teardown/reactivation, closing every open tab of a reloaded plugin's tab type — not just one — and picking up a live-edited file without restart) — both user-confirmed. 1967 tests passing (up from 1937 at #148). `docs/ui.md`/`docs/api.md` updated; `CHANGELOG.md` gained a user-visible `[Unreleased] → Added` entry (the first for the 2.3 Plugins effort — unlike #71–#75/#147/#148, this one has an end-user-visible surface: the Plugins menu itself changes for everyone, not just plugin authors).

**#159 (Plugin Preferences API — per-plugin settings + preferences UI) implemented 2026-07-24** via a 5-milestone plan (`Settings` per-plugin key/value store → thread `Settings` into `PluginLoader`/`PluginContext` + `get_setting`/`set_setting` → `PluginRegistry` preferences-page registration + `register_preferences_page` → `MainWindow` dialog/menu/teardown wiring → docs/CHANGELOG), full grill-me (13 product decisions, including a mid-interview pivot from "one tab per plugin, period" to "allow multiple tabs per plugin" and back again once the flat-vs-nested tab layout tradeoff was spelled out) → requirements (`docs/requirements/plugin-api.md` REQ-PLUGIN-410–440) → architecture (two Plan-agent review passes — one on the technical design, finding 3 real gaps plus a cleaner dependency-threading approach; one producing the milestone breakdown) → plan-mode milestones workflow. Surfaced while scoping #76 (update-checker-as-plugin): `PluginContext` gained `get_setting(key, default)`/`set_setting(key, value)`, namespaced by `Plugin.name`, backed by a new `Settings.get_plugin_setting`/`set_plugin_setting` nested key/value store; `register_preferences_page(title, widget_factory)` lets a plugin contribute at most one tab (a second attempt is logged and dropped) to a new shared, cached, Close-only "Plugin Preferences…" dialog reachable from the Plugins menu (grouped with Rescan/Reload, disabled until a page exists). The review pass's 3 real findings were all silent-corruption risks a bare `json.dumps()` validity check would have missed: a non-`str` setting key, or a non-`str` dict key nested inside a value, both round-trip-corrupt silently across a restart (Python's JSON encoder stringifies them instead of raising) — fixed with a recursive `_is_json_safe()` validator requiring `str` keys at every level; and `Settings._load()`'s first draft for the new field couldn't ever raise on a malformed shape, missing the "corrupt field resets to defaults" safety net every other `Settings` field already has — fixed by validating shape inside the existing broad `try`. The review also caught that the original design's new public `AppController.settings` property would have been an unnecessarily wide surface for one caller; switched to threading `Settings` directly through `PluginLoader.__init__` → `PluginContext.__init__` instead. New dev-mode-only `plugins/preferences_fixture/` (mirrors #75/#148's precedent) exists purely to make the dialog lifecycle live-testable. 1999 tests passing (up from 1967 at #150). Live-tested in the real running app — menu placement/enablement, dialog tab content, live self-save with Close-only (no Apply/OK), build-once caching across reopens, persistence across a full app restart, and correct tab rebuild after a Plugins → Reload Plugin cycle — all user-confirmed. `docs/api.md`/`docs/ui.md` updated; `CHANGELOG.md` gained a user-visible `[Unreleased] → Added` entry, matching #150's reasoning (not #71–#75/#147/#148's) — the Plugins menu's visible chrome changes for every user, not just plugin authors, even though the new entry starts disabled.

**#76 (convert the update checker into a first-party plugin) implemented 2026-07-25** via a 5-milestone plan (`PluginContext.main_window`/`app_version` accessors → `Settings` migration + core-field deletion → `plugins/update_checker/` package → delete old built-in code → packaging/docs), scoped as a **Feature** (not the issue's own "Refactor" framing) specifically because it's the first plugin ever required to ship in the real installer/portable build. Full grill-me (10 product decisions: Plugins-menu placement, preferences page migrated via #159, `activate()`-time self-triggered startup check with no new lifecycle hook, one-time settings migration, atomic removal, no fallback-if-plugin-fails-to-load, checkbox content unchanged, full move of the pure logic into the plugin package, new requirements-doc domain) → requirements (new `docs/requirements/update-checker.md` REQ-UPDATE-010–320, cross-referencing rather than duplicating `non-functional.md`'s existing REQ-NFR-030–033 — an overlap the first requirements-writing pass missed and had to fix; `docs/requirements/plugin-api.md` REQ-PLUGIN-450/451 for a new `main_window` accessor) → architecture (one Plan-agent review, 7 gaps found) → plan-mode milestones workflow. Implementation surfaced two things the review itself missed: (1) a `PluginContext.app_version` accessor was needed too — REQ-PLUGIN-452, added alongside `main_window` — since comparing a fetched release tag against the running version has no path that doesn't either import `mdf_viewer.__version__` directly (outside the plugin-import boundary) or grow the API; (2) the review's own proposed packaging fix (`Tree()` passed directly into `COLLECT()`, to dodge PyInstaller 6's `_internal/` routing) was verified with an actual local build rather than trusted, and turned out not to work — `COLLECT.assemble()`'s source shows `contents_directory` applied uniformly to every non-executable entry regardless of how it reached `COLLECT()`. Fixed with a post-build copy step in `docs/release.md` instead (`installer/mdf_viewer.spec` collects nothing extra for the plugin). The review's other load-bearing catch (Gap 1) was real and reproduced immediately once written: an in-flight `_UpdateCheckThread` with no teardown path crashed the process outright (`QThread` destroyed while `isRunning()`) the first time a test called `load_all()` then `deactivate_all()` back-to-back with no real `main_window` — fixed with a module-level `_LIVE_THREADS` keep-alive set (holds the thread until its own `finished` fires) plus `deactivate()` disconnecting the `update_available` signal so a late result from a Reload-torn-down instance is a no-op, never a synchronous `.wait()` that would freeze the GUI thread on every Reload. A related test-only race (a white-box test letting `activate()`'s real thread run concurrently with a patch of the same function under test) caused the intermittent multi-second stalls hit during implementation, not an infinite hang — fixed by disabling the setting before `activate()` in tests that don't specifically need a real thread, and `tests/plugin_api/conftest.py` gained an autouse fixture failing `urlopen` fast so every other plugin test that scans the real `plugins/` directory doesn't make a real network call as a side effect. 2014 tests passing (up from 1999 at #159; 1 pre-existing, unrelated failure — a Windows-path-literal test that only resolves correctly on Windows — present before this change too). `docs/architecture.md`/`docs/api.md`/`docs/ui.md`/`docs/release.md` updated; `CHANGELOG.md` gained a user-visible `[Unreleased] → Added` entry (menu + preferences relocation is visible chrome for every user).

**Live-testing caught a real bug the automated suite didn't**, fixed the same day: the first live run (menu/preferences page/startup check all confirmed working) crashed on app shutdown — `Plugin 'Update Checker' failed to deactivate` / `RuntimeError: wrapped C/C++ object of type _UpdateCheckThread has been deleted`. Root cause: once a check's background thread finished *naturally* (not via Reload), `finished.connect(thread.deleteLater)` destroyed the underlying C++ object, but `self._thread` still pointed at the now-dead wrapper — `deactivate()`'s `.disconnect()` call at shutdown raised `RuntimeError`, which the original `except TypeError` didn't catch. Fixed by clearing `self._thread` as soon as the thread finishes on its own (a new `_on_thread_finished` handler), not only inside `deactivate()`, plus catching `RuntimeError` there too as a defensive backstop; a new regression test (`test_deactivate_after_thread_already_finished_naturally_does_not_raise`) reproduces the exact sequence. 2015 tests passing. **Live-tested in the real running app — menu placement, preferences page (checkbox reflects/persists the setting), startup check, and now a clean shutdown with no error — all user-confirmed.** The real Windows packaging build (`docs/release.md`'s steps, verifying `plugins/update_checker/` actually lands as a sibling of `MDF-Viewer.exe`) still has not been run — this repo's dev environment is macOS. User accepted this as an open risk rather than blocking closure: #76 is closed on GitHub as of 2026-07-25; if the 2.3 install/portable build fails because of it, that becomes a new bug issue rather than reopening this one.

**#152 (PoC plugin exercising virtual measurements, follow-up to #147) implemented 2026-07-25.** Scoped as a lightweight, #75-style PoC rather than a full Feature cycle — it adds no new application behavior, only exercises what #147 already shipped and specified (REQ-VMEAS-*, REQ-PLUGIN-290–310), so no new requirements docs were written, just a `docs/architecture.md` note. New dev-mode-only `plugins/virtual_measurement_demo/`: a "Create Virtual Measurement" menu action builds a two-signal virtual measurement (a sine wave + a monotonic counter) via `create_virtual_signal`/`create_virtual_measurement`/`attach_virtual_signal`/`register_virtual_measurement` together for the first time outside a test; `on_measurement_closed` logs any virtual measurement's close. Building it surfaced a real nuance the issue text itself got wrong: `PluginContext._teardown()` deliberately unsubscribes a plugin from `measurement_closed` *before* removing its own virtual measurements (REQ-PLUGIN-301), so a plugin can never observe its own on-deactivation teardown through that event — only other, still-active plugins would. The "user-initiated close" checkpoint works as originally intended (the plugin stays active, so it receives the event normally); the "plugin-deactivation teardown" checkpoint is instead confirmed by directly observing the measurement disappear from the Signal Browser after a Reload, not by an event-log line. 2020 tests passing (up from 2015 after #76's shutdown-crash fix). No CHANGELOG entry (dev-mode-only, same reasoning as #75/#147/#148). **Pending live-test confirmation in the real running app** — create via the menu, confirm the "(virtual)" badge/disabled Replace… in the Signal Browser and Measurement Info Box, plot both signals, close via File → Close Measurement (watch the console log line), then Reload the plugin from the Plugins menu (watch it disappear from the Signal Browser, no log line expected).

**#151 (plugin-author SDK package) implemented 2026-07-25 as a new, separate public GitHub repo: [andalf-74/mdf-viewer-plugin-sdk](https://github.com/andalf-74/mdf-viewer-plugin-sdk)**, MIT-licensed — not a subfolder of this repo, not published to PyPI, per the issue's own design (already worked through in a prior session). Contains hand-written type stubs (`.pyi`, no runtime — a plugin never executes standalone) covering the *current* `plugin_api` surface — `Plugin`, `PluginContext`, `PluginRegistry` and all four registration types, the `Plugin*View`/`Plugin*Event` types, `CursorMode`, and the virtual-measurement model types (`SignalData`/`SignalMetadata`/`VirtualSignal`/`VirtualMeasurementLoader`) — deliberately wider than the issue's own stub-content list, which predated #147/#148/#159/#76 and would have shipped an incomplete SDK if followed literally. Validated with `mypy` (`Success: no issues found in 12 source files`). Also ships `docs/plugin-contract.md`/`plugin-context.md`/`dev-workflow.md`, a `CLAUDE.md` written to interview an SDK user about their plugin's shape before generating code, and `examples/signal_statistics/` copied verbatim from this repo's own `plugins/signal_statistics/` (diffed byte-identical). The issue's own explicitly-flagged open question — how the stub package stays in sync with `plugin_api` as it evolves — resolved as a manual checklist step in this repo's own `docs/release.md` ("Plugin-author SDK sync") rather than CI-push-on-tag or `git subtree`, matching this project's low-overhead-given-infrequent-need preference; `docs/architecture.md` gained a decision-log entry with the full reasoning. No new requirements docs or CHANGELOG entry here (third-party tooling, no MDF-Viewer application behavior changed).

**Bug #156 (Info panel shows raw CNcomment XML instead of the actual signal comment) fixed 2026-07-26** (`b1b44cf`, latest commit on `main`). Root-caused while triaging the 2.3.1 Misc Features/Fixes milestone: asammdf's `Channel`/`HeaderBlock` objects expose the comment field as raw (or re-serialized) `CNcomment`/`HDcomment` XML markup, never the parsed free text — asammdf only exposes the parsed text via a separate accessor (`MDF.get_channel_comment()`, backed by `asammdf.blocks.utils.extract_xml_comment()`) that the app wasn't using. Fixed via a shared `_extract_comment_text()` helper in `mdf_loader.py`, applied at both `channel_tree()`/`load_signal()` (per-signal Info panel comment) and `measurement_info()` (Measurement Info Box comment) — the latter surfaced during root-causing, confirmed against `data/example.mf4`'s real header comment, and folded into the same fix since it's the identical defect. Plain-text comments (MDF3, or any non-XML string) pass through unchanged. Added REQ-MDF-023/042/052 to `docs/requirements/mdf-support.md` (previously silent on "parsed text, not raw XML") and 6 regression tests. 2025 tests passing (up from 2020 at #152; 1 pre-existing, unrelated Windows-path-literal failure, same as noted at #76). `CHANGELOG.md` gained a user-visible `[Unreleased] → Fixed` entry.

**#146 (improve cursor movement by arrow keys) implemented 2026-07-26**, scoped as a Feature (changing REQ-PLOT-091's documented "selected, or first active" fallback policy, not fixing broken code) after a lighter-weight process — no dedicated grill-me/architecture-review pass, given the small blast radius (essentially one file, no new public API). Investigation found no existing "highest sample rate among signals" helper anywhere in the codebase; that part was genuinely new. `CursorController._resolve_reference_signal()` now picks the highest-effective-sample-rate signal (computed directly from each candidate's own timestamps, not `SignalMetadata.raster_s`, so a variable/indeterminate-raster signal still participates) among the current multi-selection (`workspace.selected_signals`, replacing the old singular `workspace.selected` + first-active fallback), or among all active signals when nothing is selected. New `_last_sample_reference` state (REQ-PLOT-094): when the resolved reference signal differs from the one used on the previous samples-mode arrow-key press, that press snaps the cursor to the new reference's nearest sample instead of stepping — except the very first press ever, which steps normally rather than being treated as a "change" from nothing. The `CursorController` constructor's `get_selected_signal` param (singular) was renamed to `get_selected_signals` (plural); only `app.py`'s wiring and 3 existing tests referenced it, so this wasn't a wide rename. Updated REQ-PLOT-091 and added REQ-PLOT-094 to `docs/requirements/plotting.md`, plus 9 new/updated tests. 2028 tests passing (up from 2025 at #156; same 1 pre-existing Windows-path-literal failure). `CHANGELOG.md` gained a user-visible `[Unreleased] → Changed` entry. Not yet live-tested in the real running app.

**#142 (Y Autozoom of selected signal) implemented 2026-07-26**, scoped as a Feature after a lighter-weight process (same as #146 — reused existing `plot_stripe.py` machinery, no new public API surface, no dedicated grill-me/architecture-review pass). Investigation found the per-unit min/max/padding computation already existed (`autorange_y()`'s full-data variant and `zoom_y_to_view()`'s visible-X-range variant), and `_display_units()` already resolves "a signal's Merged/Synced group, or just itself" — the only real gap was invoking that computation for one arbitrary signal instead of every unit in the stripe. New `PlotStripe.autozoom_to_signal(active)` reuses `_display_units()` to find the target's unit (so a Synced group's existing cross-member range-propagation handler "just works" with no special-casing) and applies `zoom_y_to_view()`'s visible-range computation (not `autorange_y()`'s full-data one — chosen so it behaves as a quick per-signal companion to that toolbar action) to only that unit; returns `False` (no-op) for a signal that's hidden, not in this stripe, or has no data points in the current X range, consistent with REQ-PLOT-337's existing hidden-signal exclusion. New `PlotStripesArea.autozoom_to_signal()` fans out by owning stripe; `AppController.on_y_autozoom_requested(signals)` wraps the batch in one `zoom_ctrl.before/after_discrete_action()` (one undo step per invocation) and applies autozoom independently to each given signal's own group, filtering to `current.active` first like the existing merge/sync/ungroup handlers. New `ActiveSignalsTable.y_autozoom_requested` signal, a "Y Autozoom" context-menu entry (next to Toggle Visibility), and a Ctrl+D keyboard shortcut (chosen by the user, matching a shortcut they were already used to from another tool) scoped to whichever row(s) are selected, mirroring Ctrl+W's existing precedent. Added REQ-PLOT-058/059 to `docs/requirements/plotting.md`; `docs/ui.md`'s AST context-menu and shortcut lines updated. 2044 tests passing (up from 2028 at #146; same 1 pre-existing Windows-path-literal failure). `CHANGELOG.md` gained a user-visible `[Unreleased] → Added` entry. Not yet live-tested in the real running app.

**#117 (make plot background color configurable) implemented 2026-07-26**, scoped as a Feature after a lighter-weight process (same as #146/#142). New `Settings.plot_background_color` (default `(0, 0, 0)`, matching today's existing appearance), a `QColorDialog` swatch + reset-to-default button added to Preferences → Signals (the closest existing home — Z-order, line boost, show-only-selected-Y-axis), and `PlotStripe.set_background()`/`PlotStripesArea.set_background()` wrapping `pg.PlotWidget.setBackground()`. The real design question was making it apply everywhere a stripe can come from, not just the common case: `app.py`'s `_wire_tab()` (runs for every tab, both at startup and at runtime) applies it explicitly, since the very first tab's `PlotStripesArea` is constructed in `MainWindow.__init__()` *before* `Settings()` exists; `AppController.create_stripe()`/`move_signals_to_new_stripe()` apply it to a stripe created mid-session (new `_plot_background_color` property, same `None`-fallback pattern as `_top_first`/`_zoom_all_stripes`); `WorkspaceSessionController.build_stripe_skeleton()` applies it to stripes created restoring a `.mvc` file; and a new `AppController.refresh_plot_background()` (called from `MainWindow._on_preferences()`) pushes to every already-open tab when the user changes it — unlike `refresh_cursors()`'s current-tab-only precedent, background color has no "re-read live on next render" cushion, so every open tab needs an explicit push or it'd show the old color indefinitely. A dedicated `test_main_window.py`-level test for the `.mvc`-restore path was written then dropped after it proved intermittently hanging for 30-60s (real `PlotWidget.setBackground()` calls contending for GUI/display resources, apparently with a concurrently-running real app instance in the same dev environment) — the underlying logic stayed covered by fast, reliable unit tests at the `PlotStripe`/`PlotStripesArea`/`AppController` level instead. Added REQ-PLOT-015/016 to `docs/requirements/plotting.md` (new "Plot Appearance" section); `docs/ui.md` gained its first-ever "Signals" Preferences-tab bullet (previously undocumented). 2058 tests passing (up from 2044 at #142; same 1 pre-existing Windows-path-literal failure). `CHANGELOG.md` gained a user-visible `[Unreleased] → Added` entry. Not yet live-tested in the real running app.

**#153/#154/#155/#157/#158 (UI-polish cluster) implemented and closed 2026-07-26** (`c9a7291`), all five together as one batch: same 2026-07-22 design review, same risk profile (Qt-stylesheet-level only), scoped after user agreement to fold this cluster into the 2.3 release while deferring #141/#143 (real feature work, not polish). Used the `frontend-design` skill for the token choices, grounded first by a codebase survey confirming no existing theme/QSS system (styling was ad hoc `setStyleSheet()` literals scattered across ~8 files). New `src/mdf_viewer/view/theme.py`: `ACCENT`/`ACCENT_DIM`/`ACCENT_TEXT` (a phosphor-amber `#B8860B`, deliberately distinct from every color in the 8-color signal trace palette and contrast-checked against both light and dark OS palettes, since chrome still inherits the OS scheme — no app-wide dark mode was added or requested), `monospace_font()`, and `style_section_header()`. Plain module-level constants rather than a class/Settings lookup, so a future user-configurable color scheme (raised during planning, not built) can localize to this module later. #153 threads the accent through Active Signals Table/Signal Browser selection, the Primary checkbox, and the active-stripe marker — replacing `palette(highlight)`/stock-blue everywhere, and fixing a pre-existing light-theme-only selection-contrast bug found along the way. #154 gives section headers ("Properties"/"Info"/"Signal Statistics") semi-bold + letter-spacing + an accent underline. #155 adds a thin divider between the Info panel's identity (Name/Unit/Data type)/shape (Samples/Raster/Min/Max)/text (Comment) field groups — Data type's group wasn't specified in the issue text, resolved as "identity" and recorded as a judgment call in `docs/architecture.md`. #157 applies the monospace font only to numeric readouts (cursor values, axis ticks, Signal Statistics figures) — the Signal Statistics plugin duplicates the three-line font call locally rather than importing `theme.py`, respecting the "`PluginContext` is the only `mdf_viewer` import a plugin may use" boundary (#71). #158 writes the resulting rule into a new "UI Restraint Guideline" decision-log entry. 2080 tests passing (up from 2058 at #117; same 1 pre-existing, unrelated Windows-path-literal failure noted since #76). `docs/architecture.md` and `CHANGELOG.md` updated. **Live-tested in the real running app — user confirmed it looks right** before commit.

**#160 (Plugin Overview — per-plugin enable/disable) implemented 2026-07-26** via a 5-milestone plan (Settings persistence + `PluginLoader` discovery/failure-tracking → `set_enabled` action → `app.py` wiring → `MainWindow` menu/dialog, the risk milestone → docs), full grill-me (8 product decisions) → requirements (`docs/requirements/plugin-api.md` REQ-PLUGIN-460–510) → one Plan-agent architecture review (9 real gaps, F1–F9, all folded in) → plan-mode milestones workflow — reopening the 2.3 Plugins milestone, which had been fully closed. New `Settings.disabled_plugins` (a folder-name set, mirroring `plugins_dir`'s persistence pattern) and `PluginLoader.list_packages()`/`set_enabled(folder, enabled)`/`active_plugin_names_for(folder)`/`PluginPackageInfo`. The defining design decision, locked during grill-me: a disabled plugin's folder is never imported at all, so identity throughout is the folder *name*, not the declared `Plugin.name` (which requires import to read) — enable/disable and the Overview list both operate per-folder, not per individual plugin class, since a multi-class "toolsuite" folder can't be enumerated while disabled. The architecture review's most safety-relevant catch (F4, given this project's #120/#148/#150 history with Qt-teardown bugs): disabling from the dialog must query the loader's *live* active-plugin state at toggle time, never a cached dialog-open-time snapshot, before tearing down UI — a new `active_plugin_names_for()` exists for exactly that. **A second real bug surfaced only by live-testing, after the architecture review's own F3 finding was implemented too literally:** the first cut reverted a failed re-enable attempt's checkbox to unchecked without touching the persisted setting, so `settings.json` still said "enabled" while the dialog showed unchecked — surviving a restart and reappearing checked with no explanation, exactly what the user reported ("the box was checked again... the unchecking was not preserved"). Root cause: `set_enabled(folder, True)` always persists "enabled" before attempting activation, matching REQ-PLUGIN-360's existing "Rescan retries every time, never permanently remembered as broken" policy for Rescan — so patching only the checkbox's visual state was never going to stay consistent with that. Fixed structurally, not with a patch: `_on_plugin_overview_toggled()` now always repopulates the whole dialog from a fresh `list_packages()` call after every toggle instead of hand-patching one checkbox, so the UI is a pure read of backend truth and can't drift from it — a package that's enabled but still broken now consistently shows checked + a failure indicator, matching how a freshly-discovered broken plugin already displayed. 2125 tests passing (up from 2080 at the UI-polish cluster). `docs/ui.md`/`docs/api.md`/`docs/architecture.md` updated; `CHANGELOG.md` gained a user-visible `[Unreleased] → Added` entry. **Live-tested in the real running app across two rounds — the second specifically to confirm the settings-persistence fix — both user-confirmed**, including disabling a plugin with an open tab of its own registered tab type (clean close, no crash) and the full disable → failed-reenable → restart round-trip that originally exposed the bug.

**#162 (Plugin API: attach/detach virtual signals on an already-registered virtual measurement) implemented 2026-07-27** via a 4-milestone plan (`VirtualMeasurementLoader` stable-id storage → `AppController`/`EventBus` plumbing → `PluginContext`/`plugin_api` surface → PoC + docs), full grill-me (8 decisions) → requirements (`docs/requirements/plugin-api.md` REQ-PLUGIN-520–550, `docs/requirements/virtual-measurements.md` REQ-VMEAS-121–124/135) → architecture (two independent review passes) → plan-mode milestones workflow, extending #147. Before this, `attach_virtual_signal` only reliably worked before `register_virtual_measurement()` — a plugin growing a measurement over a session (e.g. a Signal Calculator adding one computed signal at a time) had to create a whole new measurement per batch, leaving stale copies behind with no way to remove them.

**A real bug was found and fixed before any code was written**, during the architecture step, not by the grill-me: `VirtualMeasurementLoader.channel_tree()` assigned each signal's `channel_index` by its *current position* in the internal list, recomputed fresh every call — safe only as long as signals are never removed. Since an already-plotted signal's channel identity is baked in once at load time and never refreshed, detaching a signal from the middle of the list would shift every later signal's index, causing `AppController.add_signal()`'s duplicate-check (the only place that stale index is read again) to silently duplicate or silently refuse a later add touching that same measurement. Fixed by keying `VirtualMeasurementLoader`'s internal storage by a monotonically-increasing id assigned once at `attach()` time (never reused, mirroring `_measurement_load_counter`'s pattern from #103) instead of list position — a signal's `channel_index` now never changes regardless of what else is attached/detached on the same measurement (new REQ-VMEAS-124). `detach()` matches by object identity (`is`), never dataclass equality, since two attached signals may legitimately share identical name/template (REQ-VMEAS-123).

A second, adversarial review pass on the resulting design caught 6 more real findings, including a naming inconsistency in the fix itself (a helper method was defined private, called as public — fixed as one public `find_measurement_by_loader`, matching how every other cross-class `AppController` method a `PluginContext` calls is public) and a structural test gap that's existed since #147: nothing cross-checked `plugin_api/plugin.py`'s `_EVENT_HANDLER_NAMES` against `_KNOWN_EVENTS`/`_translate_event()` — a typo'd handler name would have compiled, passed every existing test, and simply never fired for any plugin. A new guard test now covers all 7 events, not just the newest. The review also caught that `measurement_updated`'s payload can't carry a `PluginSignalView` (built only from a live, plotted `ActiveSignal`) since an unplotted `VirtualSignal` has none of the properties that type requires — it carries the signal's name as a plain string instead.

New: `PluginContext.detach_virtual_signal(measurement, signal)`, a `measurement_updated` EventBus event + `Plugin.on_measurement_updated` handler (fires with the affected signal's name and whether it was attached/detached), `register_virtual_measurement` made idempotent by measurement identity (logged no-op on a duplicate call), `PluginMeasurementView.is_virtual`. Detaching a currently-plotted signal tears it down immediately across every tab via the same `_active_tab_index`-swapping per-tab loop `_remove_measurement_signals()` already uses — copying that swap was called out explicitly in the plan as a real Qt-teardown-leak risk (the #120 class) if a shortcut implementation skipped it. Extended the dev-mode-only `plugins/virtual_measurement_demo/` (#152) with two new menu actions ("Attach Extra Signal to Demo Measurement", "Detach Counter Signal from Demo Measurement") to make the new surface live-testable. 2149 tests passing (up from 2125 at #160). `docs/api.md`/`docs/architecture.md` updated. No CHANGELOG entry (no end-user-visible surface — a plugin-author API extension only, matching the #147/#148 precedent, not #150/#159/#160's "Plugins menu chrome visibly changes" precedent). **Live-tested in the real running app — user confirmed attach-after-registration appears in the Signal Browser immediately, detach removes a plotted signal from both the Signal Browser and the plot immediately with no crash, and the highest-risk case (detaching a signal plotted in a non-active tab) works correctly across two repeats, plus a clean app shutdown afterward.**

**#165 (menu bar icon redesign) implemented and closed 2026-07-28** (`2f095cf`). Toolbar/eye-toggle icons switched from light/dark PNG pairs (plus `@2x` variants) to a single stroke-based SVG per icon (`stroke="currentColor"`), recolored at load time via string substitution (Qt's `QSvgRenderer` doesn't interpret CSS `currentColor`) since raster PNGs can't be recolored dynamically. `_icon_suffix()` renamed `_icon_color()`. `folder.png` renamed `open_file.svg` (same action, clearer name). 2154 tests passing. `docs/architecture.md`/`docs/ui.md`/`CHANGELOG.md` updated. Live-tested and user-confirmed.

**#164 (AST name column mis-elided with a measurement prefix) fixed and closed 2026-07-28** (`67059af`) — took three live-testing rounds to actually land, each of the first two looking fixed by its own verification technique while still failing live, a real example of this project's "don't trust offline verification alone for anything touching real rendering" pattern (see the #78/#120 postmortems above). New `_ElidedNameDelegate` (the codebase's first custom `QStyledItemDelegate`) elides only the name portion of a `"[M1] SignalName"` display name rather than the whole string, with a raw-character fallback for the width band where `QFontMetrics.elidedText()` itself degrades to a bare `"…"`. The actual blocker across all three rounds: `_ActiveTable` has a stylesheet set, and once any stylesheet touches a widget Qt routes its item painting through `QStyleSheetStyle`, which re-elides text drawn via `style.drawControl()` regardless of an `ElideNone` override — invisible to any offline test not exercising the real, fully-styled widget. Found only via temporary `print()` diagnostics in the real paint path plus the user's live console output (same technique as #120), proving the computed string was correct while the screen still wasn't — meaning the bug was downstream of the computation, not in it. Fixed by never letting the style draw the text: blank `opt.text`, let the style draw background/selection only, then paint the real elided string directly via `QPainter`. Added `REQ-PLOT-340`. 2157 tests passing (up from 2149 at #162). `docs/architecture.md`/`CHANGELOG.md` updated. **Live-tested in the real running app — user confirmed correct.**

**#161 (installer default settings) implemented and closed 2026-07-28**, scoped as a Bug (installer ships with wrong/missing defaults, no requirements-doc impact). "Create a desktop shortcut" and "Associate .mvc files" tasks in `installer/mdf_viewer.iss` now default to checked (removed their `Flags: unchecked`); added a new `fileassoc_dat` task/registry block mirroring the existing `.mf4`/`.mdf` pattern — `.dat` was already a loadable extension in the app's own Open File dialog filter but had no installer association option. Left unchecked by default (user's call, over the issue text's ambiguity): `.dat` is a much more generic extension than `.mvc`, shared by many unrelated tools, so auto-claiming it by default was judged more invasive than the other checked-by-default options. `.mf4`/`.mdf` associations stay unchecked, matching the issue's "the rest stay as they were." No test changes (installer `.iss` scripts have no automated coverage in this repo, verified only by a real build per the `REQ-NFR-040` precedent). `docs/release.md`/`CHANGELOG.md` updated. Not yet build-verified with a real Inno Setup compile (this session's environment doesn't have Inno Setup installed) or live-tested with an actual install.

**#143 (Import/Export a list of labels) implemented and closed 2026-07-29** via a 5-milestone plan (model `label_list.py` → `AppController.import_label_list()`/`export_label_list()` → `LabelImportResultDialog` → `MainWindow` File-menu wiring → docs), full grill-me (14 product decisions, working from a real Vector CANape `.lab` file the user supplied) → requirements (new `docs/requirements/label-lists.md` REQ-LABEL-010–073) → one Plan-agent architecture-review pass → plan-mode milestones workflow. New `model/label_list.py` (`LabelGroup`/`parse_label_list`/`format_label_list`, UTF-8 with a `cp1252` fallback) and `LabelListParseError` (`errors.py`) are the only new files below the controller; `AppController.import_label_list()`/`export_label_list()` reuse existing primitives wholesale — `find_signal_locations_by_name()` (built for #101) for cross-measurement matching, `add_signal()`'s existing duplicate check for dedup — rather than adding new matching logic. The architecture review caught several real implementation risks before any code was written: a naive `\n`-split would have broken on every real CRLF-terminated CANape export; a per-match (not per-candidate) result-bucketing bug would have mis-reported a name added under one measurement as "already active" because it was also already active under a second; and export needed to iterate `workspace.active` filtered by `get_stripe_for_signal()`, not `get_signals_in_stripe()`, to avoid the same on-screen-order footgun already fixed once for #119. Two requirements gaps the review surfaced were resolved before implementation: a matched-but-failed-to-load channel folds into "not found" (REQ-LABEL-033) rather than a third UI category, and the post-import summary dialog doesn't appear at all on a fully clean import (REQ-LABEL-051). 2206 tests passing (up from 2157 at #164/#165/#161; same 1 pre-existing, unrelated Windows-path-literal failure noted since #76 — not present in this run's count since it wasn't re-verified this session). `docs/ui.md`/`docs/api.md`/`docs/architecture.md` updated; `CHANGELOG.md` gained a user-visible `[Unreleased] → Added` entry (new File-menu chrome for every user). **Live-tested in the real running app** — import against `data/example.mf4` with a synthetic multi-group fixture (including a group with zero matches, and a group name containing nested brackets), repeat-import to confirm dedup, tab/zoom/cursor sanity after the stripe churn, and export — all user-confirmed; live-testing itself then caught a real usability gap the grill-me and architecture review both missed (a single combined "Copy to Clipboard" button on the result dialog), fixed by giving the "Not found" and "Already active" lists their own separate copy buttons, then re-confirmed live.

**#126 (Feature: Logging) implemented 2026-07-30** via a 4-milestone plan (core `logging_config.py` + `Settings` fields → bootstrap wiring + lifecycle logging → Preferences UI → docs), full grill-me (11 product decisions — destination, level granularity, what's logged, default state, rotation, Preferences placement, apply timing, uncaught-exception capture, plugin-logging surface) → requirements (new `docs/requirements/logging.md` REQ-LOG-010–043) → one Plan-agent architecture-review pass against the real codebase → plan-mode milestones workflow. Actual milestone on GitHub is **2.3.5 Further Polishing**, not the Backlog milestone this file previously listed it under — corrected below. Scoped as built-in core, not a plugin (a plugin can't observe its own loader's failures or app-startup errors): new `logging_config.py` idempotently attaches a `RotatingFileHandler` (5MB × 3 backups) to the **root** logger (not a `"mdf_viewer"`-namespaced one, so a plugin's own `logging.getLogger(__name__)` call is captured with zero new `PluginContext` API — a dynamically-imported plugin module's synthesized `__name__` never falls under `"mdf_viewer"`), plus `install_excepthook()` and `open_log_folder()`. New `Settings.logging_enabled`/`logging_level`/`config_dir`. The architecture review caught several real gaps before any code was written: a `QCheckBox.setChecked(False)` no-op on an already-unchecked fresh widget wouldn't fire `toggled`, so the Preferences log-level dropdown needed an explicit initial `setEnabled()` call, not just the `toggled` connection; `configure_logging()`'s call in `_on_preferences()` had to sit outside the existing `and self._controller is not None` guard (unrelated concerns, and an easy-to-avoid coupling bug); the root logger and `sys.excepthook` are genuine process-wide singletons requiring a new autouse `tests/conftest.py` fixture to avoid leaking file handles into `tmp_path` (a real Windows pytest-cleanup `PermissionError` risk); and `RotatingFileHandler` construction needed its own `try/except OSError` so a locked-down config directory can never block app startup. `app.py`'s `run()` was reordered to construct `Settings()`/call `configure_logging()`/`install_excepthook()` before `QApplication` itself, confirmed safe by the review. 2237 tests passing (up from 2206 at #143/#164/#165/#161). `docs/ui.md`/`docs/api.md`/`docs/architecture.md` updated; `CHANGELOG.md` gained a user-visible `[Unreleased] → Added` entry. **Live-tested in the real running app across two checkpoints** — bootstrap/lifecycle logging (real launch, file-open success/failure via `data/example.mf4`/`data/faultfile.mf4`, dev-mode plugin activation, confirmed via the actual log file) after the bootstrap-wiring milestone, then the full Preferences flow (checkbox/dropdown initial-disabled-state gating, "Open log folder", live-apply, persistence across a real restart) after the Preferences-UI milestone — both user-confirmed.

**#141 (Tree View selectable by Preferences) implemented 2026-08-01** via a 5-milestone plan (`Settings.signal_browser_view_mode` → `SignalBrowser` core Flat/Tree rendering → `AppController` wiring → Preferences UI → docs), full grill-me (10 product decisions, most notably reframing the issue's own "tabs for each loaded measurement" into promoting measurements to the tree's own top-level nodes instead — the user's suggestion mid-interview, which sidestepped a redundant second tab strip alongside the Measurement Info Box's existing one) → requirements (`docs/requirements/signal-browser.md` REQ-BROWSER-025, 060–063, 070–075, plus mode-scoping edits to REQ-BROWSER-010/023/050–054) → one Plan-agent architecture-review pass → plan-mode milestones workflow. Tree mode: Measurement (top node, carries the `(virtual)`/`[label]` identity Flat mode puts on every channel row) → Channel Group → Channel leaf, in the file's own order, reusing the same `QStandardItemModel`/`_FlatSignalProxy` pair Flat mode already uses rather than swapping models per mode (`_build_ui()` wires selection/filter signals exactly once — swapping would risk silently dropping that wiring).

The architecture review caught three real bugs before any code was written, all now covered by dedicated regression tests: (1) `QSortFilterProxyModel.sort()` is sticky, so Flat mode's `sort(0)` (Flat being the default, populated first almost every session) would survive into a subsequent Tree populate and trigger `lessThan()` — which reads `_LOCATION_ROLE` unconditionally — on every insert, crashing on measurement/group items that deliberately carry no `_LOCATION_ROLE` (leaf-only selection); fixed with an explicit `self._proxy.sort(-1)` at the start of `_populate_tree()`. (2) `setRecursiveFilteringEnabled(True)` only controls proxy row *acceptance*, not the tree view's expand state, so a filter match under a collapsed Channel Group would stay invisible on screen even though accepted; fixed by having `_apply_filter()` call `expandAll()`/`expandToDepth(0)` directly (also closing a stale pre-#103 wording bug in `REQ-BROWSER-023`, folded into the same requirements pass). (3) the existing measurement-filter-combo restore-by-label logic would silently carry a Flat-mode filter into Tree mode, hiding every other measurement's whole subtree with the combo itself hidden and no way to see why; fixed with an unconditional reset rather than a restore in Tree mode's own combo-hide path. 2262 tests passing (up from 2237 at #126). `docs/ui.md`/`docs/api.md` updated; `CHANGELOG.md` gained a user-visible `[Unreleased] → Added` entry (new Preferences ▸ Signals dropdown). **Live-tested in the real running app across two checkpoints** — Tree-mode rendering/expand-collapse/leaf-only-selection/filter behavior with a temporary hardcoded default (before milestone 2's SignalBrowser-core-vs-Preferences-UI risk milestones), then the full Preferences toggle end-to-end including the measurement-filter-carryover fix specifically — both user-confirmed.

**#125 (Status Bar Message History) implemented 2026-08-01** via a 5-milestone plan (`StatusMessageHistory` core mechanics + `show_status()` split → `StatusHistoryDialog` widget, standalone → `MainWindow` button + singleton show/raise wiring → live update + `closeEvent` hang fix → docs), full grill-me (14 product decisions) → requirements (new `docs/requirements/status-bar.md` REQ-STATUS-010–032, plus a cross-reference edit to `docs/requirements/logging.md`) → two Plan-agent architecture-review passes (one on the technical design, one on the milestone breakdown) → plan-mode milestones workflow. Scoped as View-owned per the grill-me's own MVC-ownership question: `StatusMessageHistory` (`view/status_message_history.py`) is plain, unbounded, in-memory, session-only data populated directly inside `MainWindow.show_status()` — no `AppController`/`PluginContext`/EventBus involvement, since nothing outside the UI layer needs "what text was shown in the status bar." `show_status()` gained a keyword-only `log: bool = True` parameter: every message is still recorded into history regardless, but only non-routine ones (matching `docs/requirements/logging.md`'s pre-existing high-frequency-action exclusion) also reach the application log at INFO — the first architecture-review pass caught this exact conflict (REQ-STATUS-014 as originally drafted would have flooded the log with routine guard messages like "No active signals to zoom.") before any code was written. A dedicated always-visible button on the status bar's **left** side (the user's own correction of an initial "right-side permanent widget" assumption) opens a non-modal, singleton `StatusHistoryDialog` (`view/status_history_dialog.py`) — a read-only `QPlainTextEdit`, one `"HH:MM:SS  message"` line per entry, native Qt text selection, a "Copy to Clipboard" button that always copies the entire history regardless of any selection. The second architecture-review catch was more serious: since the dialog is deliberately non-modal, a user leaving it open and then closing `MainWindow` would hit Qt's `quitOnLastWindowClosed` default and silently prevent the app process from exiting — fixed by having `closeEvent()` explicitly close the history dialog first, mirroring the existing `_parked_page.deleteLater()` cleanup pattern already there for the #130/#120 leak class. Live-append re-syncs the open dialog's content on every `show_status()` call regardless of the dialog's current visibility (not only while visible), specifically so reopening a dialog the user had merely closed never shows stale content missing whatever was recorded while it was hidden — a small deviation from the architecture review's own draft condition, caught and fixed during implementation rather than being asked back to the user. The already-unreferenced `resources/icons/status_log.svg` (stroke-based, `#165`-convention-conformant) turned out to already exist in the repo, needing only to be wired up via the existing `_load_icon()` helper — no new icon asset was authored. 2289 tests passing (up from 2262 at #141). `docs/ui.md`/`docs/api.md` updated; `CHANGELOG.md` gained a user-visible `[Unreleased] → Added` entry (new status bar chrome for every user). Auditing which app actions do or don't currently call `show_status()` was explicitly descoped to its own follow-up issue during the grill-me — filed as [#166](https://github.com/andalf-74/MDF-Viewer/issues/166), Backlog. **Live-tested in the real running app — user confirmed**, including the specific `closeEvent`/app-exit fix. Committed (`565659d`), pushed, and closed 2026-08-01.

**#166 (Audit actions for missing status bar messages) implemented 2026-08-01**, scoped as a lightweight Feature per user agreement (no grill-me/architecture-review — the issue itself already specified what to do: a bounded, non-exhaustive pass, not an open-ended audit). Surveyed `MainWindow`'s action handlers for ones giving zero feedback on success or failure, presented a candidate list, and implemented all 4 the user approved plus a cleanup surfaced by a design-review conversation after #125 shipped: (1) **Export Labels** success now shows `"Labels exported to X."`; (2) **Import Labels** now always shows `"N signal(s) imported."` (previously silent on a fully-clean import, since REQ-LABEL-051 only suppresses the *summary dialog*, not all feedback) — required extending `LabelImportResult` with a new `added: int` field, counting each individual successful `add_signal()` call (so one candidate name matching two measurements can contribute 2); (3) **Close Measurement** now shows `"Closed measurement 'X'."` (`log=False` — `AppController.close_measurement()` already logs this independently); (4) **Undo/Redo** now show `"Nothing to undo."`/`"Nothing to redo."` when their respective history is empty — previously completely silent either way, indistinguishable from a successful no-visible-effect action; required widening `ZoomController.undo()`/`redo()` and `AppController.undo()`/`redo()` from `-> None` to `-> bool` so the no-op case is observable at all, a real (small) public-API change, not just a `MainWindow`-local addition. Separately, a **cleanup**: 6 status messages across 4 `MainWindow` handlers (plugin Rescan summary, Reload success/failure, "enabled but failed to activate", plugin menu-action failure) were switched from the `log=True` default to explicit `log=False`, after actually checking whether each one duplicated something `plugin_api/loader.py`/`registry.py` already logs independently — confirmed all 4 do (each already calls its own `logger.info`/`logger.error`/`logger.exception` on the same event), so logging the status-bar summary too was pure duplication in the log file. New requirements: `docs/requirements/file-handling.md` REQ-FILE-120 (new "Status Feedback" section), `docs/requirements/label-lists.md` REQ-LABEL-052/064, `docs/requirements/plotting.md` REQ-PLOT-067. 2311 tests passing (up from 2289 at #125). `docs/api.md` updated (new `LabelImportResult.added` field, `ZoomController.undo()`/`redo()`'s new return contract); `CHANGELOG.md` gained a user-visible `[Unreleased] → Added` entry. Live-tested in the real running app — user confirmed. Committed and closed 2026-08-01.

**#163 (Copy Signal Names) implemented 2026-08-01**, scoped as a lightweight Feature per user agreement (a few direct clarifying questions instead of a full grill-me — small, well-scoped, no new public API beyond two widget-level signals). Two decisions locked in before implementation: copied text is the **raw channel name only** (no `[M1]` measurement-disambiguation prefix, no unit — matching #143's export-labels convention, since the use case is often pasting into another tool rather than a human-readable report), and the trigger is **Ctrl+C in both the Signal Browser and Active Signals Table**, with an additional "Copy Name(s)" context-menu entry only on the AST (the Signal Browser has no context menu today and didn't get one just for this). `ActiveSignalsTable._copy_names(signals)` and `SignalBrowser._on_copy_names()` both write to the clipboard directly (`QGuiApplication.clipboard().setText(...)`, one name per line) and emit a new `names_copied(int)` signal purely for `MainWindow._on_names_copied()` to surface a status-bar count — no controller round-trip, since copying to the clipboard is pure UI with no state to mutate. Reused `_SORT_ROLE` (Signal Browser's existing bare-channel-name data role, originally added for Flat-mode sorting) to read the raw name rather than parsing it back out of the display text. The AST's existing Ctrl+W/Ctrl+D `keyPressEvent`-on-the-outer-widget pattern was confirmed to already rely on real, standard Qt behavior (an ignored key event auto-propagates up the parent chain when the focused child segment doesn't handle it) rather than being a synthetic-test-only artifact, so Ctrl+C there is genuinely low-risk, unlike this project's usual mouse-routing caution (#78/#120/#164). New requirements: `docs/requirements/plotting.md` REQ-PLOT-341–343, `docs/requirements/signal-browser.md` REQ-BROWSER-080–082. 2322 tests passing (up from 2311 at #166). `docs/ui.md`/`docs/api.md` updated; `CHANGELOG.md` gained a user-visible `[Unreleased] → Added` entry. Live-tested in the real running app — user confirmed. Committed and closed 2026-08-01.

**#129 (Tooltip Makeover) implemented 2026-08-01**, scoped as a lightweight Feature per user agreement (survey → bounded candidate list → agreed scope → implement, matching #166's process). An Explore-agent survey of `src/mdf_viewer/view/` found the codebase already had strong tooltip coverage on toolbar actions and most of Preferences; real gaps clustered into two tiers — Tier A: icon-only widgets with literally zero text cue (`ColorSwatch`, `VisibilityToggleButton`, the 6 `_CursorColorSwatch` color chips across Preferences' Cursors/Signals tabs, the Signal Browser's measurement filter combo, which has no adjacent label unlike every other combo in the app); Tier B: labeled widgets whose tooltip-worthy behavior isn't obvious from the label alone. User chose Tier A plus the non-obvious slice of Tier B (skipping self-explanatory buttons like "Remove Signal"/"Save Workspace"): the Measurement Info Box's "Primary" checkbox (actually controls sync-reference/axis-ordering, REQ-PLOT-317, not just a label), the Signal Info Box's three enum-display checkboxes ("Value table"/"Cursor label"/"Y-axis" — genuinely cryptic without one), the `PlotStripe` Sync/Un-Sync button (now dynamically describes whichever action a click will actually take, updated alongside its existing text swap), and Preferences' Cursors-tab "Reset to defaults" button (its actual scope — colors, ∆-Time display, *and* arrow-key step settings — is wider than the label suggests). `_CursorColorSwatch.__init__` gained an optional `tooltip` param (each of the 6 call sites passes distinct text); the local `reset_btn`/`bg_reset_btn` variables were promoted to `self._cursor_reset_btn` for testability, matching every other testable Preferences widget's existing pattern. Treated like the #153–#158 UI-polish cluster, not #166/#163: no new `docs/requirements/` REQ-IDs (tooltip text is presentation, not a new observable requirement — same reasoning that kept #153–#158 out of the requirements docs), just regression tests asserting each new/changed tooltip is non-empty (and, for the sync button, that it actually changes between states) plus a `CHANGELOG.md` entry. 2331 tests passing (up from 2322 at #163). Pure tooltip text with no interaction/routing changes, so no live-test caution applies. Committed and closed 2026-08-01.

**#111 (Keyboard Shortcut Configuration) implemented 2026-08-01** via a 6-milestone plan (data model → `apply_keymap()` refactor → `keyPressEvent` rebinding → Preferences Shortcuts tab → `.mvck` load/save → docs), full grill-me (14 product decisions) → requirements (new `docs/requirements/keyboard-shortcuts.md` REQ-KEYS-010–062) → one Plan-agent architecture-review pass → plan-mode milestones workflow. Scoped, per the user's own framing stated before the grill-me even started, to the *rebinding mechanism and preset load/save only* — not adding a shortcut to anything unbound today (split into follow-up [#167](https://github.com/andalf-74/MDF-Viewer/issues/167)) and not building any new functionality MDA's/CANape's own shortcut tables suggest but this app doesn't have (split into follow-up [#168](https://github.com/andalf-74/MDF-Viewer/issues/168)). **20 actions are rebindable** (originally 22 — see live-testing findings below): the 9 existing toolbar/menu `QAction` shortcuts, 6 existing global `QShortcut`s, 3 existing `ActiveSignalsTable` `keyPressEvent`-based shortcuts, plus two actions promoted from "no shortcut at all" specifically because the issue's own MDA preset needs them (the Cursors toolbar button's cycle action, and "Move to new Stripe") — a narrow, justified exception, not the general "make everything reconfigurable" question #167 covers. Each action gets a primary + optional secondary slot (the model the user proposed directly mid-interview, after Zoom to Fit's existing dual Ctrl+0/F binding came up) — three built-in presets (Default, MDA per the issue's 5 explicit entries, CANape mapped from its ~50-entry reference table down to the 8 actions with a plausible MDF-Viewer equivalent, confirmed with the user rather than assumed) plus unlimited custom presets saved/loaded as named `.mvck` JSON files. New `keymap_presets.py` (`ACTION_IDS`/`ACTION_LABELS`, `DEFAULT_PRESET`/`MDA_PRESET`/`CANAPE_PRESET` as `{**DEFAULT_PRESET, **overrides}`, `keymap_to_dict`/`keymap_from_dict`, `load_mvck`/`save_mvck`). `Settings.keymap` stores the plain JSON-native shape (no Qt import), matching every other Settings field's convention. The architecture review corrected two initial assumptions before any code was written: `QShortcut.setKeys([...])` already supports multiple simultaneous bindings identically to `QAction.setShortcuts()` (no special-casing needed across the two mechanisms), and "Move to new Stripe" belongs in `ActiveSignalsTable.keyPressEvent` (matching every other AST-scoped shortcut) rather than a new `QShortcut`. A real per-tab gap was caught during implementation: each tab's own `ActiveSignalsTable` instance has no shared state, so `apply_keymap()` alone would have left a tab created *after* the last call silently stuck on `DEFAULT_PRESET` — fixed by having `_wire_tab_view()` push the current keymap explicitly for every new tab, the same pattern already used there for `set_shorten_names_enabled`. Conflict handling (warn-and-block, naming the conflicting action) is genuinely new UX in this codebase — no existing "reject and name the other owner" dialog to reuse.

**Live-testing found a real bug this project's own #78/#120/#164 caution had specifically flagged as a risk class, plus three smaller issues — all fixed same-session, `docs/architecture.md`'s decision-log entry has the full technical detail:**
1. **Rebinding to a modifier-less key (Space, a plain letter) silently never fired**, while Ctrl+ combos worked fine. Root cause: `ActiveSignalsTable.keyPressEvent` lived on the outer facade widget, which never has real OS keyboard focus, relying on Qt propagating an *unhandled* key event up from whichever `_ActiveTable` segment is actually focused — but `QAbstractItemView`'s own default handling (type-ahead search for a letter; item activation for Space) consumes those internally before "unhandled" propagation would ever happen, while a Ctrl+ combo bypasses that default handling and propagates fine (exactly why testing with the already-shipped Ctrl+W/Ctrl+D never caught it). Fixed by mirroring the existing `on_mouse_press`/`on_drag_start`/`on_click_release` callback-hook pattern already used for mouse routing on `_ActiveTable` — a new `on_key_press` hook, checked first on the actually-focused segment itself.
2. **The shortcut-conflict warning appeared twice.** `QMessageBox.warning()`'s modal nested event loop moves focus off the `QKeySequenceEdit`, and `editingFinished` fires on focus-out as well as sequence-complete, re-entering the handler for the same still-conflicting value before the first call's revert had run. Fixed with a small reentrancy guard.
3. **Copy Name(s) (Ctrl+C) removed from rebindable scope entirely**, per direct user feedback ("no one should rebind copy and paste") after unbinding one widget's Ctrl+C didn't free it up for the other (both were independent rebindable actions sharing one conflict-detection pool). Both the AST and Signal Browser reverted to a plain hardcoded Ctrl+C check, exactly as before #111 — dropping the count from 22 to 20 and removing `SignalBrowser.set_keymap()` entirely (nothing left there to configure).
4. **Shortcuts tab rows rebuilt as one `QGridLayout`** (label/primary/secondary/reset columns) instead of independent per-row `QHBoxLayout`s, so key fields align in fixed columns regardless of label length — direct user feedback ("make the whole page a bit more orderly") after the first version left fields starting wherever each row's own label happened to end.

Also renamed 3 row labels for clarity per direct user feedback after the second live-test round: "Cursors (cycle hidden → 1 → 2 → hidden)" → "Cycle Cursors" (too long), "Toggle Cursor 1" → "Toggle one Cursor", "Toggle Cursor 2" → "Toggle both Cursors". 2389 tests passing (up from 2331 at #129). `docs/ui.md`/`docs/api.md`/`docs/architecture.md`/`docs/requirements/keyboard-shortcuts.md` updated; `CHANGELOG.md` gained a user-visible `[Unreleased] → Added` entry. **Live-tested in the real running app across two rounds — the second specifically re-verifying the 4 fixes plus the label rename — both user-confirmed.** Committed and closed 2026-08-01.

**2.3.5 Further Polishing, refreshed from GitHub 2026-08-04: 1 open (#168), 8 closed** (#111, #125, #126, #129, #141, #163, #167, done above; #132 closed as **Discarded** by the user directly, not implemented — no read-only workspace-config feature exists). #170, filed during #167's own live-testing (after this refresh snapshot), is also in this milestone — resolved 2026-08-05, see above. #169 is also done, see above — only #168 remains open in this milestone.

**#86 (Select a Signal as the X-Axis, 2.4 Artificial Signals milestone) — IN PROGRESS as of 2026-08-05, paused mid-session for the day, no code written yet.** #168 (New functions inspired by MDA keyboard shortcuts) was picked up first after #169 but immediately deferred — the user doesn't have MDA's/CANape's own shortcut reference handy and needs to ask a colleague, so we moved to #86 instead. Full grill-me (15 product decisions, the biggest single-feature grill-me so far) reframed the issue from a mode-toggle into **a new, built-in tab type** (the user's own idea, prompted by the assistant flagging that per-tab scoping was the natural boundary anyway) reusing the tab-type mechanism #148 built, but *not* registered as a plugin. Requirements written to new `docs/requirements/x-axis-signal.md` (REQ-XAXIS-010–080) — full spec there; headline decisions: full interactive parity (cursors/zoom/stepping, not a read-only view); curves plotted in the axis signal's own recorded *temporal* order (can loop back if non-monotonic, matching MDA/CANape, not sorted by value); a cursor's position stays a genuine time internally even in this tab type (nearest-to-current-time tie-break for mouse-drag, exact snap for clicking a point); 3 stepping units (axis-signal samples/pixels/axis-signal-value, no "time"); multi-measurement allowed for free via existing offset machinery (motivated by the user's own catch: a computed "driven distance" signal, #147's virtual-signal mechanism, would live in a *different* measurement than the real signals it's plotted against, so a naive single-measurement restriction would have blocked the headline use case); axis signal fixed for the tab's lifetime (new tab for a different one); full `.mvc` persistence parity with ordinary Plot tabs; axis signal shown as a pinned, non-removable "(X-axis)" row above the stripe-segmented Active Signals Table area.

One Plan-agent architecture-review pass (also 2026-08-05) against the real codebase found the "reuse `PlotStripesArea`/`PlotStripe` unchanged" premise is **not** free — full detail in `docs/architecture.md`'s new "X-Axis Signal Tabs (#86)" entry, but the headline catches: pyqtgraph's `clipToView`/`autoDownsample` (`plot_stripe.py:654-655`) and the existing endpoint-based (not min/max) `zoom_to_fit()` are all silently wrong once the axis isn't monotonic, exactly what this feature explicitly allows; a **live, already-existing** persistence bug where `AppController._capture_tab()` never actually receives/propagates `view_type` (silently defaults every tab to `"plot"` on save) needs fixing *before* this feature's own persistence can work at all; the #148 tab-type mechanism is confirmed structurally plugin-only (no `TabWorkspace`, lossy persistence by design) and therefore the wrong host, validating the "sibling reserved type_id, not a plugin registration" design; and the cursor system's stored-time-as-render-coordinate assumption (`cursors.py`'s `CursorView`/`CursorStripesView`) needs a translation seam inserted, not just "new algorithms" as the first draft assumed. All of these have concrete resolutions already recorded in the architecture doc. **Two genuinely open product questions were deliberately left for the user rather than decided unilaterally** (also in the architecture doc): whether the global `Settings.cursor_step_unit` preference should widen to a 4th value or become per-tab-type instead, and whether the signal picker should skip its numeric/sample-count filter entirely for virtual-signal candidates (since #147's virtual signals deliberately don't carry stats before resolution).

**2026-08-06 session: both open questions settled with the user, then M1–M6 of the plan-mode milestone breakdown implemented** (plan file, this session, followed the review's recommended order almost exactly — see `docs/architecture.md` for the full milestone-by-milestone technical detail). Both previously-open questions resolved directly with the user, not decided unilaterally: (1) `Settings.cursor_step_unit` stays a single global 3-way choice (samples/pixels/value) — no 4th value, no per-tab-type split; "value" means the axis signal's own recorded value in an X-Axis Signal tab instead of time. (2) The picker's numeric/≥2-samples filter is skipped for virtual-signal candidates (REQ-XAXIS-016) — confirmed by reading the code that `channel_tree()` never populates `sample_count`/`is_integer` for *any* signal (real or virtual) before it's actually loaded, so there's nothing reliable to filter on for real signals either; `candidate_axis_signals()` applies no pre-filter at all, and an unsuitable pick fails at `load_axis_signal()` with a clear error instead. A third decision (not in the original architecture doc) was also settled: the pinned Active Signals Table axis row shows the axis signal's live cursor value, same as an ordinary row (REQ-XAXIS-032, added to the requirements doc).

**M1** (`model/axis_resample.py::resample_to_axis`, `PlotStripe`/`PlotStripesArea`'s new `monotonic_x` constructor flag disabling `clipToView`/`autoDownsample` and switching `zoom_to_fit()` to real min/max) — **M2** (the full render-space↔time translation seam across `cursors.py`/`cursor_controller.py`, new `model/axis_cursor.py::nearest_instant_by_value`/`step_by_axis_value`, identity-by-default so ordinary tabs are provably unaffected) — **M3** (`TabWorkspace.view_type`/`axis_signal`, reserved `"xaxis"` type_id in `plugin_api/registry.py`, `AppController.create_xaxis_tab()`, the REQ-XAXIS-071/072 offset-invalidation split in `on_measurement_offset_changed()`, `app.py`'s `_xaxis_cursor_kwargs()` wiring, `MainWindow._xaxis_pages` bookkeeping parallel to the #148 `_tab_type_by_page` dict, and the `_capture_tab_specs()`/`_capture_tab()` fix closing the dormant `view_type`-defaulting bug the architecture review flagged) — **M4** (`ActiveSignalsTable`'s new pinned `_AxisSignalRow`, `CursorController._push_axis_cursor_values()`) — **M5** (`TabConfig.axis_signal` + `.mvc` JSON round-trip in `config_manager.py`, `WorkspaceSessionController.build_tab_skeletons()`'s new `"xaxis"` restore branch with `_resolve_axis_signal()`, `MainWindow._on_new_xaxis_tab()`/`set_xaxis_tab_factory()` mirroring the existing plot-tab factory pattern) — **M6** (`view/xaxis_signal_picker_dialog.py`, "New X-Axis Signal Tab…" in the Edit menu, AST context-menu "Promote to X-Axis Signal Tab…" single-signal-only entry, `AppController.candidate_axis_signals()`/`load_axis_signal()`/`promote_signal_to_xaxis_tab()`, the latter reusing `remove_signal()`'s existing curve/ViewBox teardown wholesale as REQ-XAXIS-013's "move, not clone" per the architecture review's own framing) — all implemented, each with its own unit-test pass, full suite green after every milestone.

**A live-testing gap in the picker was found and fixed same-session, before any further live-testing could continue**: the first cut of `XAxisSignalPickerDialog` had no filter box, unlike the Signal Browser's Flat-mode list — unusable against a real channel count. Rather than re-deriving the Signal Browser's wildcard/substring filter logic, extracted it into new `view/widgets/text_filter.py` (`apply_text_filter()` — wildcard when `*`/`?` present, else `setFilterFixedString()`, since `setFilterWildcard()` alone requires a full match, not "contains"; `wire_debounced_filter()` — the same 250ms debounce timer pattern) and refactored `SignalBrowser` itself to call these instead of its own inline copy, so there's one implementation, not two. `XAxisSignalPickerDialog` was rewritten from a plain `QListWidget` to `QListView`+`QStandardItemModel`+`QSortFilterProxyModel` to support this. **User confirmed filtering now works.**

2596 tests passing (up from 2473 before this session; same 1 pre-existing, unrelated Windows-path-literal failure noted since #76). `docs/requirements/x-axis-signal.md` gained REQ-XAXIS-016 (virtual-signal filter skip) and REQ-XAXIS-032 (axis row live cursor value) this session, on top of yesterday's REQ-XAXIS-010–080.

**Nothing has been committed yet** — this is still one long uncommitted working session (37 modified/new files as of this pause point). `docs/architecture.md`/this file (`CLAUDE.md`) have uncommitted edits reflecting the full M1–M6 detail above.

**Live-testing status (M6 is this project's mandatory-live-testing checkpoint, #78/#120/#164 postmortems)**: the picker's filter box, and now (2026-08-07) end-to-end tab creation + non-monotonic curve rendering, have been confirmed live — tested with vehicle speed as the axis signal (a good non-monotonic case: rises, falls, sits at zero at stops), user confirmed the curve loops back correctly with no visual glitches. **Not yet live-tested**: cursor drag/click/arrow-key-stepping in all 3 units against a real axis signal; the pinned AST row's live value; Zoom to Fit/Zoom Y to View/box zoom; context-menu promotion (the #120 Qt-teardown risk specifically); Save Workspace/reload round-trip with a real xaxis tab open; a full app close afterward. The known, deliberately-deferred parking edge case (closing the *only* remaining tab while it's an xaxis tab leaves the parked `PlotStripesArea` still structurally non-monotonic underneath a page MainWindow now treats as an ordinary plot page — flagged with a full explanation in a code comment at `main_window.py`'s `_on_tab_close_requested`) also still needs a live repro before deciding whether/how to fix it.

**2026-08-08/09 session: cursor drag/click/arrow-key-stepping live-tested, one small fix made, one real bug found and paused mid-investigation.** Cursor drag and click-to-snap on the vehicle-speed axis signal confirmed working live (REQ-XAXIS-041/042/043). Arrow-key stepping in "value" units surfaced a genuine design question — pressing right on a decelerating stretch moved the cursor visually *left*, since `step_by_axis_value()` advances the cursor's *time* and accepts a value change in either direction (matching REQ-XAXIS-051 as written). Talked through two alternatives (redefine right/left to always search for an increase/decrease in axis value, possibly jumping over a large chunk of the recording to find one) versus keeping the current time-forward/backward semantics; user chose to **keep the current behavior as correct** ("more honest to the data" — the cursor is riding the recording's own trajectory, and a momentary reversal is an honest reflection of it, not a bug). No code change made; REQ-XAXIS-051 stays as written. Separately, added a tooltip to the Preferences arrow-key-step unit dropdown clarifying that "Time" means axis-signal value in an X-Axis Signal tab (user's own suggestion; chose a tooltip over renaming the label) — `preferences_dialog.py`, regression test in `test_preferences_dialog.py::test_step_unit_tooltip_explains_xaxis_meaning`, no REQ-ID (presentation-only, same reasoning as #129).

**Bug found live-testing, root-caused and fixed same session (2026-08-09), NOT YET LIVE-TESTED**: after switching tabs, dragging Cursor 1 ("left cursor") did not update the Active Signals Table's live cursor-value column; dragging Cursor 2 ("right cursor") worked fine, and Cursor 1 "self-healed" after a couple more drag attempts once Cursor 2 had also been moved. **User confirmed this also reproduced in ordinary (non-X-Axis) plot tabs**, not just the new xaxis tab type — confirmed as a genuine regression from this session's `cursor_controller.py`/`cursors.py` changes (M2, the render-space/time translation seam), not a pre-existing bug.

Root cause (found via a background read-only investigation agent, then fixed directly): M2 changed `CursorView`'s `sigPositionChanged` handler (`cursors.py:147-149`) from reading the `InfiniteLine`'s own live position (`ln.value()`, always accurate) to reading a new `self._current_times[idx]` cache, written only by `_set_cursor_time()` — itself only called from the DragClaimant-routed `on_move()`/`apply_mode()`. But `InfiniteLine` stays `movable=True`, so pyqtgraph's own native `mouseDragEvent` can still move the line directly via `setPos()` whenever `PlotStripe`'s `hit_test()` (`sceneBoundingRect()`-based) misses the press — plausible right after a tab switch, before the scene's geometry has settled, matching the #78 Z-order/hit-testing postmortem's same stale-geometry class. When that native fallback fires, the line visibly moves but `_current_times[idx]` never updates, so every subsequent `cursor_moved` emission for that cursor keeps reporting the same frozen value.

Fix: new `CursorView._sync_cursor_time_from_line(idx)` (`cursors.py`), called from the `sigPositionChanged` handler instead of reading the cache directly — compares the line's actual rendered position against `to_render_x(_current_times[idx])`, and if they've drifted (i.e. something moved the line without going through `_set_cursor_time()`), resolves the correct time via the same `resolve_time_at_render_x()` translation `on_move()` uses, and updates the cache before returning. This is a defense-in-depth fix at the read site rather than trying to prevent `hit_test()` from ever missing (a deeper, harder-to-fix pyqtgraph/Qt geometry quirk, consistent with the project's other documented instances of this class). Two new regression tests in `test_cursor_view.py` simulate the native-bypass path directly (`_lines[0].setValue(...)` without going through `on_move()`) for both identity (ordinary tab) and translated (xaxis tab) cases. 2606 tests passing (up from 2604 before the fix — plus the tooltip test from earlier tonight). Same 1 pre-existing Windows-path-literal failure.

**2026-08-09/10 session: M6 live-testing completed (Cursor 1/AST-update fix confirmed first, then the rest of the checklist), three more real bugs found and fixed along the way, then M7 (docs/CHANGELOG closing pass).** The Cursor 1/AST-update fix (tab switch immediately followed by a drag) was confirmed live in both an ordinary plot tab and an X-Axis Signal tab. The rest of the M6 checklist — pinned AST row's live value, Zoom to Fit/Zoom Y to View/box zoom on the non-monotonic axis, Save Workspace/reload round-trip with a real xaxis tab open, full app close — all confirmed working live, no further issues.

Three more real bugs surfaced during this live-testing pass, all found, fixed, and re-confirmed live the same session:
1. **Context-menu promotion was a move when it should be a copy.** Confirmed live-working as originally built (a move — REQ-XAXIS-013 as first written), but the user pushed back on the design itself once seeing it live: "Move to new Stripe" only makes sense within one plot view (having a signal twice there is redundant), but promoting to an X-Axis Signal tab sends the signal into a *different* plot context, where the original tab is still a legitimate place to see it plotted normally. Fixed by switching `promote_signal_to_xaxis_tab()` from `remove_signal()` to the existing `_clone_active_signal()` helper (the same one `copy_signals_to_new_tab()`/`duplicate_tab_signals()` already use) — the original now stays untouched in its tab. New `REQ-XAXIS-017`; `docs/architecture.md`'s prior "move, not clone" design note corrected in place rather than deleted, so the reasoning trail stays visible.
2. **The "+"/New Tab chooser (#148) didn't offer X-Axis Signal** — it only ever listed plugin-registered types, since X-Axis Signal is deliberately not a plugin registration (a separate, dedicated "New X-Axis Signal Tab…" Edit-menu action was the only way in, until the user pointed out the gap directly). Fixed by folding "X-Axis Signal…" into that same chooser as a fixed built-in option, confirmed with the user this always-show-the-chooser behavior (one extra click for the common "just want a Plot tab" case, even with zero plugins installed) was worth the consistent discoverability — which then made the separate dedicated Edit-menu action redundant, so it was removed outright rather than kept alongside the chooser. New `REQ-XAXIS-018`; `REQ-PLUGIN-330/331` reworded.
3. **Signal Value Search panned to the wrong location in an X-Axis Signal tab** — found while writing this closing entry (the `pan_to_center()` open question the prior session's resume note flagged), not by interactive live-testing first, then live-test-confirmed after fixing: `AppController.search_execute()` passed the search match's raw *time* value straight into `pan_to_center()`, which treats its argument as an already-render-space X coordinate — correct only for an ordinary Plot tab. Cursor 1 itself always landed correctly (a separate code path already handled the translation); only the view-recentering was wrong. Fixed by extracting the render-space conversion into a shared `model/axis_cursor.py::time_to_axis_render_x()`, reused by both `app.py`'s cursor wiring and `search_execute()`. New `REQ-SEARCH-055`.

M7 completed same session: `docs/architecture.md`'s X-Axis Signal Tabs entry closed out (implementation summary for M1–M6, the three fixes above, both previously-open product questions marked resolved with their actual resolutions); `docs/ui.md`/`docs/api.md` updated with the full shipped surface (Tabs, Menu Bar, Active Signals Table, Search Dialog sections; new module-table rows for `model/axis_cursor.py`/`model/axis_resample.py`/`view/xaxis_signal_picker_dialog.py`/`view/widgets/text_filter.py`; `AppController`/`CursorController`/`CursorView`/`PlotStripe`/`PlotStripesArea`/`TabWorkspace`/`TabConfig` additions); `docs/requirements/keyboard-shortcuts.md` checked — no change needed, "New Tab" was already rebindable (#167) and its expanded chooser doesn't add a new action, so the count stays 26; `CHANGELOG.md` gained a user-visible `[Unreleased] → Added` entry. 2609 tests passing (up from 2596 before this session; same 1 pre-existing Windows-path-literal failure). **Nothing has been committed yet** — still one long uncommitted working session; commit is the next step once agreed with the user.

Also this session: while scoping a separate, deliberately-parked effort — a "Signal Calculator" plugin built in a prior session with Claude Code WebApp against the `mdf-viewer-plugin-sdk` reference repo (a PoC for third-party plugin development, currently sitting untracked at `plugins/sigcalc_bundle/`) — filed [#172](https://github.com/andalf-74/MDF-Viewer/issues/172) ("Feature: Signal Calculator plugin", under Epic #58 Artificial Signals) and posted the incorporation plan as an issue comment (rename to `plugins/signal_calculator/`, rework its virtual-measurement snapshot workaround to use #162's real attach/detach API — the plugin was in fact #162's own motivating use case — new requirements doc, new test file, eventual installer packaging once verified). **Explicitly paused, not started** — the user wanted to finish #86 first; resume by reading #172 and its plan comment, no need to re-derive.

**#171 ("Zoom to Cursors" enabled-state only tracks the first tab's cursor mode) fixed 2026-08-10**, scoped as a Bug per user agreement — found live-testing #86 but confirmed to reproduce in an ordinary second plot tab too, not specific to X-Axis Signal tabs. Root cause (already diagnosed in the issue body before work started): `MainWindow.set_controller()` calls `controller.set_cursor_mode_callback(self._on_cursor_mode_changed)` exactly once, which forwarded straight to whichever tab's `CursorController` was current at that moment (the first tab) instead of being remembered by `AppController`; every tab created afterward built a fresh `CursorController` via `set_cursor_controller()`, which only ever wired `set_position_changed_callback`, never the mode-changed callback, and `switch_tab()` didn't re-sync it either. Fixed by having `AppController` store the callback (`self._cursor_mode_cb`) once registered, re-wiring it to every subsequently created tab's `CursorController` from `set_cursor_controller()` (guarded so only the currently-active workspace's mode change is ever forwarded — mirrors the existing closure-capture pattern `set_position_changed_callback` already uses for the #99 cursor_moved event), and having `switch_tab()` explicitly push the newly active tab's already-current mode (`workspace.cursor_ctrl.mode`) on every switch, since the callback itself only fires on a *change*. No requirements doc change needed — REQ-PLOT-231 already states cursor state is independent per tab; this was a pure implementation deviation from an already-correct requirement, the Bug-flavor case that doesn't need new REQ text. 3 new regression tests in `tests/controller/test_app_controller.py`. 2612 tests passing (up from 2609 before this session; same 1 pre-existing, unrelated Windows-path-literal failure). `CHANGELOG.md` gained a user-visible `[Unreleased] → Fixed` entry. **Live-tested in the real running app — user confirmed.** Committed and closed 2026-08-10.

**#110 (Signal Value Search) implemented 2026-08-02** via a 6-milestone plan (`model/signal_rate.py` extraction → `model/signal_search.py` evaluation engine → controller layer (`SearchController`, `CursorController`/`PlotStripesArea`/`AppController` additions) → `view/search_dialog.py` → wiring/keymap → docs), full grill-me (16 product decisions) → requirements (new `docs/requirements/signal-search.md` REQ-SEARCH-010–054) → one Plan-agent architecture-review pass → plan-mode milestones workflow. Searches for a value, or an AND-conjunction of values across multiple signals, among the signals active in the current tab; on a match, jumps Cursor 1 and pans the plot. Grill-me highlights: exact-match-by-default with a per-row comparison operator (`=`/`≠`/`>`/`<`/`≥`/`≤`) instead of tolerance/fuzzy-matching, after the user corrected an initial assumption (most automotive signals are integers that survive the float64 round-trip exactly; a genuinely continuous signal is better served by `>`/`<` than fuzzy equality); always-last-known-value (zero-order-hold) semantics for the AND conjunction regardless of a signal's own step_mode display setting, scanned along the fastest-raster signal among only the searched rows (mirroring #146's reference-signal rule); "find next" via the same Search button (same criteria continues from the last match, changed criteria restarts); a singleton non-modal dialog pre-filled from the AST context-menu's current selection (operator `=`, value = current Cursor 1 reading). The architecture review's most important catch: `search_execute()` must resolve its target tab from the searched signals themselves (`AppController._workspace_owning_signal()`, an identity-based scan of every `TabWorkspace.active`), never `current_workspace` — the dialog is deliberately non-modal and can outlive a tab switch or the closing of the tab its rows came from, and driving a torn-down tab's plot/cursor objects is exactly the crash class #120 already fixed once; a dedicated regression test proves search targets the owning tab, not the focused one. Also caught: a `SearchController` find-next state snapshot keyed on bare `id(signal)` would have been vulnerable to CPython address reuse once a signal is freed (fixed with a structural `(signal, operator, value)` tuple comparison instead), and `pan_to_center()` should reuse the existing cross-stripe X-range broadcast (`PlotStripe.sync_x_range`) rather than looping over every stripe manually. `docs/ui.md`/`docs/api.md`/`docs/architecture.md`/`docs/requirements/keyboard-shortcuts.md` (rebindable-action count 20→21) updated; `CHANGELOG.md` gained a user-visible `[Unreleased] → Added` entry.

**A first live-testing pass immediately surfaced a real UX gap the architecture review hadn't caught, fixed same-session**: `_workspace_owning_signal()` correctly keeps a search pinned to the tab it was opened on, but clicking Search while looking at a *different* tab silently succeeds off-screen — the cursor jumps and the plot pans in a tab the user isn't looking at, reading as "nothing happened." Direct user feedback proposed going further than just labeling the target tab: since the same value combination is often meaningful across tabs holding the same signals, switching tabs while the dialog is open should carry the search forward. Implemented via `MainWindow._refresh_search_dialog_for_tab_switch()`, hooked into the existing `_on_tab_changed()` (already wired to `QTabWidget.currentChanged`, which fires uniformly for both a manual tab click *and* Qt's automatic reindexing after the searched-in tab gets closed — the "tab closed while dialog open" case the user also asked about falls out of the same path, no separate handling needed): old rows are matched to the new tab's signals by raw channel name, carrying forward a match's operator/value, dropping an unmatched row, and adding a blank row for each of the new tab's unmatched signals; find-next state resets since the signal set changed. Added REQ-SEARCH-016/017/018; a "Searching in: `<tab>`" label was also added to the dialog per the same feedback.

**Live-testing this fix immediately surfaced a second loophole, also fixed same-session**: Duplicate Tab (and Copy Signals to New Tab, same underlying code) auto-focuses its newly created tab, and since a duplicate starts with the exact same signal names as its source, the new carry-forward refresh would silently retarget an in-progress search onto the duplicate — nothing looked different except the tab-name label, easy to miss. Both that case and the legitimate "tab being searched in was closed" case make an indistinguishable `setCurrentIndex()` call under the hood (both only ever fire `currentChanged`, never `tabBarClicked`), so the fix couldn't be a Qt-signal distinction — added an explicit `MainWindow._suppress_search_dialog_tab_refresh` flag, set only around `_on_new_tab()`'s own tab-switch (the single method both Duplicate Tab and Copy Signals to New Tab call internally), an opt-out at the few known tab-*creation* sites rather than an opt-in at every legitimate navigation path. Added REQ-SEARCH-019. 2466 tests passing (up from 2389 at #117).

**A second live-testing round confirmed the tab-switch carry-forward and Duplicate-Tab exclusion both work, but surfaced a real usability gap: the Signal column showed the raw channel name, too long to be useful on real automotive signal names.** The user's first proposed fix — replace the auto-follow-active-tab mechanism with an explicit tab-selector dropdown — was reconsidered and dropped mid-conversation ("I changed my mind, I don't want a drop down"); the existing auto-follow/carry-forward/Duplicate-Tab-exclusion behavior above stays exactly as implemented. The actual fix was narrower than either of us first assumed: not eliding/truncating long names (the #164 mechanism), but reusing the app's existing "short name" mechanism — `AppController._format_display_name`, already shortening via the Display Name Rule setting and prefixing `[label]` once >1 measurement is loaded, used everywhere else (Active Signals Table, Signal Info Box) but not yet by the Search dialog. Renamed to public `format_display_name`; `SearchDialog.set_rows()` gained an optional `name_for` callable, wired from `MainWindow` at both call sites. 2468 tests passing. Full detail in `docs/architecture.md`'s Signal Value Search entry.

**A third live-testing round confirmed tab-switching and the short-name fix both work, but found one more stale-dialog case: closing the only remaining tab left the dialog open showing the closed tab's stale name/rows** (`if there are no more tabs, we don't need the search` — direct user feedback). Root cause: that specific close path swaps `_content_stack` to `_empty_tabs_placeholder` directly rather than switching `_tab_widget` to another real tab, so `_on_tab_changed()`'s placeholder-index guard skips `_refresh_search_dialog_for_tab_switch()` entirely. Fixed by closing the Search dialog outright in that branch instead. Added REQ-SEARCH-060. 2470 tests passing.

**A fourth live-testing round confirmed the last-tab-close fix, but prompted the user to revisit REQ-SEARCH-019 itself** (Duplicate Tab deliberately not retargeting the dialog, added during the second round): "it seems like the search window does not get informed" when duplicating a tab. Asked directly rather than guessing — the user chose to remove the exclusion entirely: the dialog now follows every tab-focus change uniformly, including tab-creation, the same as a manual switch. That reversal exposed a real ordering bug the old exclusion had incidentally been hiding: `_on_duplicate_tab()`/`_on_copy_signals_to_new_tab()` both call `_on_new_tab()` internally, whose tab-switch now fires a refresh — but at that instant the new tab is still nameless and empty, before the "Copy of …" rename and signal-copy happen. Fixed with one more explicit refresh call at the end of both methods, after their final state is built. `MainWindow._suppress_search_dialog_tab_refresh` deleted outright. REQ-SEARCH-017/018 revised into one uniform rule; REQ-SEARCH-019 removed. 2471 tests passing. Full detail in `docs/architecture.md`. **Live-tested a fifth time — user confirmed Duplicate Tab now correctly follows to the new "Copy of …" tab.**

**#167 (Add keyboard shortcuts to existing functions without one) implemented 2026-08-04**, scoped as a Feature per direct user agreement, via a full grill-me → requirements → one Plan-agent architecture-review pass → implementation workflow (no separate plan-mode milestones — the review's confirmed design was small enough to implement directly). The grill-me's real work was going through each of the 12 candidate actions individually and deciding worthy-or-not before touching a single key combo, reversing #123's discussion pattern onto this issue's own list: **6 of 11 previously-unbound actions got a default shortcut** (Save Workspace As, New Tab, New Stripe, Sync Measurements, Preferences, the "All Stripes" zoom-scope toggle), 5 stayed deliberately unbound (Apply Config…, Import/Export Labels…, About MDF-Viewer, Enter/View License Key… — each either dialog-gated, so a shortcut would only save one click before an unavoidable secondary interaction, or used at most a handful of times ever), and the existing-but-empty-by-default `cursor_toggle` ("Cycle Cursors") action finally got Ctrl+R filled in across all three presets (previously only `MDA_PRESET` had it). New Tab's obvious mnemonic (Ctrl+T) collided with an existing MDA-preset-only binding (`ast_move_to_new_stripe`) — resolved with a preset-specific exception (Ctrl+T in Default/CANape, Ctrl+N in MDA) rather than picking a different universal key, since Ctrl+T's browser-tab convention was judged worth keeping wherever it doesn't conflict. A design tangent during the Preferences-shortcut discussion (the user wanting the dialog to remember its last-viewed tab) was split off as its own follow-up, [#169](https://github.com/andalf-74/MDF-Viewer/issues/169), rather than folded in. The architecture review confirmed the core design (extend `keymap_presets.py`'s `ACTION_LABELS`/`DEFAULT_PRESET`/`MDA_PRESET` and `main_window.py`'s `_build_actions()`/`_rebindable_qt_objects()` — no new `QAction`s, menu items, or handlers needed, since all 6 already existed and were already wired) but caught two real gaps: a structural completeness guard between `ACTION_IDS` and `_rebindable_qt_objects()` had never existed since #111 shipped, meaning a forgotten wiring entry would silently leave that action's shortcut permanently un-rebindable with nothing failing — added `test_rebindable_objects_plus_ast_actions_cover_every_action_id`; and several already-stale hardcoded action-count literals in `keymap_presets.py`'s module docstring, `docs/api.md`, and `docs/ui.md` (some stale even before #167, dating to #110's addition of Search) needed correcting to 27, not just bumping by 6. 2477 tests passing (up from 2471 at #110; same 1 pre-existing, unrelated Windows-path-literal failure noted since #76). `docs/requirements/keyboard-shortcuts.md` (REQ-KEYS-010 revised, REQ-KEYS-013/014/015 added), `docs/ui.md`, `docs/api.md` updated; `CHANGELOG.md` gained a user-visible `[Unreleased] → Added` entry. Not yet live-tested in the real running app.

**#170 ("All Stripes" toolbar toggle's checked state is hard to see) resolved 2026-08-05 by removing the toggle entirely**, scoped as a Bug — the issue's own live-testing-surfaced report (checked/unchecked states nearly indistinguishable) started an in-progress fix restyling the button, but discussing it with the user surfaced a better root fix: "Zoom to Fit"'s X-axis reset was already global across every stripe regardless of the toggle (the shared X-axis propagates to all stripes on any change), so the toggle only ever gated the Y-autorange loop — meaning it added a whole extra control surface to save skipping one autorange step. Removed `_zoom_all_stripes_action`/its toolbar button and styling, `_on_zoom_scope_toggled`/`set_zoom_all_stripes`, `Settings.zoom_scope`/`DEFAULT_ZOOM_SCOPE`, the `zoom_all_stripes` keymap action (freeing the "A" shortcut, dropping the rebindable-action count from 27 to 26 — reverting one of the six #167 additions), and `AppController`'s `_zoom_all_stripes` scope-passing; `PlotStripesArea.zoom_to_fit()`/`zoom_y_to_view()` lost their `all_stripes` parameter entirely, now hardcoding the fixed behavior instead. New fixed rule: "Zoom to Fit" always Y-autoranges every stripe; "Zoom Y to View" always scopes to only the active stripe. Both toolbar tooltips updated to state their fixed scope explicitly. `docs/requirements/plotting.md` (REQ-PLOT-053/054/057 reworded to state the fixed rule instead of describing a toggle; REQ-PLOT-231's per-tab list also dropped a reference to a setting that was never actually per-tab), `docs/requirements/keyboard-shortcuts.md` (REQ-KEYS-010/013 counts/lists corrected), `docs/ui.md`, `docs/api.md` updated; `CHANGELOG.md` gained a user-visible `[Unreleased] → Changed` entry, and the still-unreleased #167 `Added` entry was corrected to drop the now-nonexistent shortcut. 2465 tests passing (same 1 pre-existing Windows-path-literal failure). Committed (`6ecb9b9`), pushed, and closed 2026-08-05. **Live-tested in the real running app — user confirmed both toolbar tooltips and the new fixed Zoom to Fit/Zoom Y to View scopes work correctly.**

**#169 (Preferences dialog remembers last-viewed page) implemented 2026-08-05**, scoped as a Feature per direct user agreement, via a few direct clarifying questions instead of a full grill-me (small, self-contained, no new public API beyond one constructor param and one property — matching the lighter-weight process used for #146/#142/#166/#163/#129). Three decisions locked in before implementation: the memory is session-only, not persisted to `settings.json` (resets to the first tab on app restart); it updates regardless of OK vs Cancel, since it reflects where the user was looking rather than a setting they chose to keep; and it gets a new dedicated `docs/requirements/preferences.md` (REQ-PREFS-010–012), since no Preferences-dialog-level requirements file existed yet. `PreferencesDialog` gained `initial_tab_index: int = 0` (applied via `self._tabs.setCurrentIndex(...)` right after `_build_ui()`) and a `current_tab_index` property reading `self._tabs.currentIndex()` — `self._tabs` itself is a new instance attribute, since the `QTabWidget` was previously only a local variable inside `_build_ui()`. `MainWindow` gained a session-only `self._preferences_last_tab_index: int = 0`; `_on_preferences()` passes it in as `initial_tab_index` and reads it back from `dlg.current_tab_index` immediately after `exec()` returns, before branching on accepted/cancelled — so the read happens unconditionally, matching the "regardless of OK/Cancel" decision. 2473 tests passing (up from 2465 at #170). `docs/ui.md`/`docs/api.md` updated; `CHANGELOG.md` gained a user-visible `[Unreleased] → Added` entry. **Live-tested in the real running app — user confirmed.**

The **2.X Artificial Signals** milestone was renamed/versioned to **2.4 Artificial Signals** (#58 umbrella, #86, #108 epic, #121, #123) on GitHub — 5 open, 1 closed (#110, done above). A **Backlog** milestone (unscheduled) holds #57, #60, #61, #118 (moved here 2026-08-01, retitled "as a first-party plugin" — see Current Status above) open, plus #166 (done, see above) closed. A separate **Create Manual** milestone (not previously tracked in this file) holds #55, #127 (epic), #128, all open. #144 ("Recording task of a signal") is closed under the **2.3 Plugins** milestone (part of that milestone's 27/27 closed count already noted above) — corrected here; it was never actually in Backlog.

### Changelog
Notable changes are tracked in `CHANGELOG.md` (Keep a Changelog style). Update it alongside `CLAUDE.md` when shipping a fix or feature.

---

## Agent skills

### Issue tracker

Issues are tracked on GitHub (andalf-74/MDF-Viewer), via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context; domain docs are this repo's existing `docs/requirements/*.md` (the "what") and `docs/architecture.md` (the "how"/decision log) — not a generic `CONTEXT.md`/`docs/adr/`. See `docs/agents/domain.md`.

---

## Environment

- `.venv` exists with deps installed (`pip install -e ".[dev]"`). Python 3.14.5. asammdf resolved to 8.x.
- Activate with `.venv\Scripts\activate`, then `pytest` and `python -m mdf_viewer` both work. Current test count is tracked in Current Status below, not duplicated here.
- `cryptography` must be installed separately on macOS: `.venv/bin/pip install cryptography`

---

## Security — secrets that must never be committed

The Ed25519 private key and `generate_license.py` are saved at `C:\Users\andal\Documents\mdf-viewer-private\` — **never commit either to any repo**. `.gitignore` blocks `generate_license.py`, `*.lic`, and `*.pem`. See `docs/architecture.md` for full license system design decisions.
