# Runtime dependencies for MDF-Viewer.
# Pinned for reproducible installs; bump deliberately.
PyQt6>=6.6
pyqtgraph>=0.13
asammdf>=7.4
numpy>=1.24
cryptography>=41
python-certifi-win32>=1.0; sys_platform == "win32"
